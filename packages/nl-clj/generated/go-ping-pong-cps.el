;;; go-ping-pong-cps.el --- GENERATED: build-time CPS transform of nl-clj-async's go/chan demo -*- lexical-binding: t; -*-

;; SPDX-License-Identifier: GPL-3.0-or-later

;;; Commentary:

;; GENERATED by `packages/nl-clj/scripts/nl-clj-async-cps-dump.el'
;; (run under Emacs 30.1) from `nl-clj-async.el's `nl-clj-async--make-
;; chan-1'/`nl-clj-async--blocking-take-1'/`nl-clj-async--blocking-
;; put-1' and `examples/nl-clj-async/go-ping-pong.el's `nl-clj-async-
;; demo-ping-pong'.  Doc 195 §4.6.
;;
;; Every `nl-clj-go'/`nelisp-actor-lambda' form has been replaced by
;; its `macroexpand-all' output under REAL `generator.el': ordinary
;; closures implementing the CPS state machine directly.  No
;; `iter-lambda'/`iter-yield'/`nl-clj-go' macro call remains anywhere
;; in this file -- `nelisp-spawn' just `funcall's whatever thunk it is
;; given and does not care how the resulting iterator was built, so
;; these plug into `nelisp-actor.el' exactly as the macro-built ones
;; do, and run on the standalone substrate via the `iter-next'/`iter-
;; close'/`iter-end-of-sequence' shim `nelisp-actor.el' installs when
;; `generator.el' is absent.
;;
;; DO NOT EDIT BY HAND.  Regenerate with `make nl-clj-async-cps-baseline'.

;;; Code:

(defun nl-clj-async--make-chan-1-standalone (capacity)
  "The real, un-baked channel-mediator constructor.  See this file's\nCommentary for why this is not literally `nelisp-make-chan'.  CAPACITY\nnil or 0 gives an unbuffered (rendezvous) channel; a positive integer\npermits that many values to queue without blocking a sender -- same\ncontract as `nelisp-make-chan'.  Returns an ordinary `nelisp-chan'\nstruct (from nelisp-actor.el); `nelisp-chan-send'/`nelisp-chan-recv'/\n`nelisp-chan-close' -- and this file's own `nl-clj->!'/`nl-clj-<!'/\n`nl-clj-close!', which are exactly those -- work on the result exactly\nas they do on a `nelisp-make-chan' channel, since\n`nl-clj-async--chan-dispatch's `:send'/`:recv'/`:close' handling is\nbyte-identical to that mediator's own.  `:recv-alt'/`:send-alt' are\nthe two new message types this package's own alts! design (Doc 195\n§4.6) adds, claim-checked through `nl-clj-async--alts-claim' (`nl-safe''s\n`nl-cell' reused as a claim-once race adjudicator, per that section's\nown text).  The actor body itself is deliberately tiny -- see\n`nl-clj-async--chan-state'/`nl-clj-async--chan-dispatch' above for why:\nthis is the ONE `nelisp-actor-lambda' this file's standalone bake\nneeds to transform (`nl-clj-async-cps-baseline'), and a small body is\nboth easier to verify by hand and a smaller CPS-transform surface.\n\nAgainst-the-bug, ONE deliberate divergence from `nelisp-make-chan'\nfound (and fixed here, not inherited) by this package's own test\nsuite: `nelisp-make-chan''s own mediator loop condition -- `(while\n(not (and closed (null buf) (null pending-senders))))' -- lets the\nactor terminate (`:dead') the moment a channel is closed AND has\nnothing buffered or pending.  That is fine for every existing\n`nelisp-actor-test.el' channel case (none of them `:recv'/`:send' a\nSECOND time after that point), but it directly breaks Clojure's own\n`<!'/`>!' contract on a closed channel: a real closed channel answers\n`<!' with nil, and `>!' with false, REPEATEDLY, forever -- not once.\nA dead mediator cannot do that; the very next `nelisp-chan-recv' on it\nsignals `nelisp-actor-error' `send-to-dead-actor' instead of returning\n`(:closed)' -- measured directly, running this package's own close!\ntests before this fix: closing an unbuffered channel with nothing\npending killed the mediator immediately, and a `nl-clj-<!' called on\nit even once more crashed the caller.  The fix: this actor's own loop\nnever exits (`(while t ...)') -- `closed' is state the dispatch\nhandlers already consult on every request (see `nl-clj-async--chan-\nhandle-recv'/`-send'/`-recv-alt'/`-send-alt' above), not a shutdown\nsignal, so serving `(:closed)'/`:error-closed' indefinitely falls out\nof the existing per-request logic with no new code; only the loop's\nown willingness to keep asking `(nelisp-receive)' for one more request\nhad to change.  The mediator actor for a channel therefore lives for\nas long as the channel itself is reachable, exactly matching a real\ncore.async channel's own lifecycle (an object that keeps answering\ncorrectly for its whole lifetime, not one that vanishes on close)."
  (let*
      ((cap (or capacity 0))
       (act
	(nelisp-spawn
	 #'(lambda nil
	     (let
		 (cps-current-state-181 cps-current-value-180
					cps-state-terminal-182
					cps-binding-state-183
					cps-state-atom-185
					cps-binding-cps-argument-186-187
					cps-state-atom-188
					cps-state-let*-189
					cps-state-atom-190
					cps-state-atom-192
					cps-state-iter-yield-193
					cps-state-atom-194
					cps-state-atom-195
					cps-state-while-191
					cps-state-while-184
					cps-state-let*-196
					cps-state-atom-197)
	       (setq cps-state-terminal-182
		     #'(lambda nil
			 (signal 'iter-end-of-sequence
				 cps-current-value-180)))
	       (setq cps-state-atom-185
		     #'(lambda nil
			 (setq cps-current-value-180
			       (prog1 t
				 (setq cps-current-state-181
				       cps-state-while-184)))))
	       (setq cps-state-atom-188
		     #'(lambda nil
			 (setq cps-current-value-180
			       (prog1
				   (let* nil
				     (nl-clj-async--chan-dispatch
				      cps-binding-state-183 cap
				      cps-binding-cps-argument-186-187))
				 (setq cps-current-state-181
				       cps-state-atom-185)))))
	       (setq cps-state-let*-189
		     #'(lambda nil
			 (progn
			   (setq cps-binding-cps-argument-186-187
				 cps-current-value-180)
			   (setq cps-current-state-181
				 cps-state-atom-188))))
	       (setq cps-state-atom-190
		     #'(lambda nil
			 (setq cps-current-value-180
			       (prog1
				   (progn
				     (nelisp-actor--mailbox-pop
				      nelisp-actor--current))
				 (setq cps-current-state-181
				       cps-state-let*-189)))))
	       (setq cps-state-atom-192
		     #'(lambda nil
			 (setq cps-current-value-180
			       (prog1
				   (null
				    (nelisp-actor-mailbox
				     nelisp-actor--current))
				 (setq cps-current-state-181
				       cps-state-while-191)))))
	       (setq cps-state-iter-yield-193
		     #'(lambda nil
			 (progn
			   (setq cps-current-state-181
				 cps-state-atom-192)
			   (throw 'cps--yield cps-current-value-180))))
	       (setq cps-state-atom-194
		     #'(lambda nil
			 (setq cps-current-value-180
			       (prog1 'nelisp-actor--receive
				 (setq cps-current-state-181
				       cps-state-iter-yield-193)))))
	       (setq cps-state-atom-195
		     #'(lambda nil
			 (setq cps-current-value-180
			       (prog1
				   (let* ((v nelisp-actor--current))
				     (nelisp-actor--set-status v
							       :blocked-receive))
				 (setq cps-current-state-181
				       cps-state-atom-194)))))
	       (setq cps-state-while-191
		     #'(lambda nil
			 (progn
			   (setq cps-current-state-181
				 (if cps-current-value-180
				     cps-state-atom-195
				   cps-state-atom-190)))))
	       (setq cps-state-while-184
		     #'(lambda nil
			 (progn
			   (setq cps-current-state-181
				 (if cps-current-value-180
				     cps-state-atom-192
				   cps-state-terminal-182)))))
	       (setq cps-state-let*-196
		     #'(lambda nil
			 (progn
			   (setq cps-binding-state-183
				 cps-current-value-180)
			   (setq cps-current-state-181
				 cps-state-atom-185))))
	       (setq cps-state-atom-197
		     #'(lambda nil
			 (setq cps-current-value-180
			       (prog1 (nl-clj-async--chan-state--make)
				 (setq cps-current-state-181
				       cps-state-let*-196)))))
	       (setq cps-current-state-181 cps-state-atom-197)
	       (let
		   ((iterator
		     #'(lambda (op value)
			 (cond
			  ((eq op :close)
			   (progn
			     (setq cps-current-state-181
				   cps-state-terminal-182)
			     (setq cps-current-value-180 nil)))
			  ((eq op :next)
			   (setq cps-current-value-180 value)
			   (let ((yielded nil))
			     (unwind-protect
				 (prog1
				     (catch 'cps--yield
				       (while t
					 (funcall
					  cps-current-state-181)))
				   (setq yielded t))
			       (if yielded nil
				 (progn
				   (setq cps-current-state-181
					 cps-state-terminal-182)
				   (setq cps-current-value-180 nil))))))
			  (t
			   (error "Unknown iterator operation %S" op))))))
		 nil iterator))))))
    (nelisp-chan--make :actor act :capacity cap)))

(defun nl-clj-async--blocking-take-1-standalone (chan)
  "The real, un-baked body of `nl-clj-<!!'.  See that function and this\nfile's Commentary."
  (let (result got)
    (nelisp-spawn
     (let ((chan chan))
       #'(lambda nil
	   (let
	       (cps-current-state-199 cps-current-value-198
				      cps-state-terminal-200
				      cps-binding-v-201
				      cps-state-atom-202
				      cps-state-let*-203
				      cps-state-atom-204
				      cps-state-atom-206
				      cps-state-iter-yield-207
				      cps-state-atom-208
				      cps-state-atom-209
				      cps-state-while-205
				      cps-state-atom-210)
	     (setq cps-state-terminal-200
		   #'(lambda nil
		       (signal 'iter-end-of-sequence
			       cps-current-value-198)))
	     (setq cps-state-atom-202
		   #'(lambda nil
		       (setq cps-current-value-198
			     (prog1
				 (let* nil
				   (progn
				     (setq result
					   (if
					       (eq
						(car cps-binding-v-201)
						:closed)
					       nil
					     (car
					      (cdr cps-binding-v-201))))
				     (setq got t)))
			       (setq cps-current-state-199
				     cps-state-terminal-200)))))
	     (setq cps-state-let*-203
		   #'(lambda nil
		       (progn
			 (setq cps-binding-v-201 cps-current-value-198)
			 (setq cps-current-state-199
			       cps-state-atom-202))))
	     (setq cps-state-atom-204
		   #'(lambda nil
		       (setq cps-current-value-198
			     (prog1
				 (progn
				   (nelisp-actor--mailbox-pop
				    nelisp-actor--current))
			       (setq cps-current-state-199
				     cps-state-let*-203)))))
	     (setq cps-state-atom-206
		   #'(lambda nil
		       (setq cps-current-value-198
			     (prog1
				 (null
				  (nelisp-actor-mailbox
				   nelisp-actor--current))
			       (setq cps-current-state-199
				     cps-state-while-205)))))
	     (setq cps-state-iter-yield-207
		   #'(lambda nil
		       (progn
			 (setq cps-current-state-199
			       cps-state-atom-206)
			 (throw 'cps--yield cps-current-value-198))))
	     (setq cps-state-atom-208
		   #'(lambda nil
		       (setq cps-current-value-198
			     (prog1 'nelisp-actor--receive
			       (setq cps-current-state-199
				     cps-state-iter-yield-207)))))
	     (setq cps-state-atom-209
		   #'(lambda nil
		       (setq cps-current-value-198
			     (prog1
				 (let* ((v nelisp-actor--current))
				   (nelisp-actor--set-status v
							     :blocked-receive))
			       (setq cps-current-state-199
				     cps-state-atom-208)))))
	     (setq cps-state-while-205
		   #'(lambda nil
		       (progn
			 (setq cps-current-state-199
			       (if cps-current-value-198
				   cps-state-atom-209
				 cps-state-atom-204)))))
	     (setq cps-state-atom-210
		   #'(lambda nil
		       (setq cps-current-value-198
			     (prog1
				 (nelisp-send (nelisp-chan-actor chan)
					      (list :recv
						    (nelisp-self)))
			       (setq cps-current-state-199
				     cps-state-atom-206)))))
	     (setq cps-current-state-199 cps-state-atom-210)
	     (let
		 ((iterator
		   #'(lambda (op value)
		       (cond
			((eq op :close)
			 (progn
			   (setq cps-current-state-199
				 cps-state-terminal-200)
			   (setq cps-current-value-198 nil)))
			((eq op :next)
			 (setq cps-current-value-198 value)
			 (let ((yielded nil))
			   (unwind-protect
			       (prog1
				   (catch 'cps--yield
				     (while t
				       (funcall cps-current-state-199)))
				 (setq yielded t))
			     (if yielded nil
			       (progn
				 (setq cps-current-state-199
				       cps-state-terminal-200)
				 (setq cps-current-value-198 nil))))))
			(t (error "Unknown iterator operation %S" op))))))
	       nil iterator)))))
    (nelisp-actor-run-until-idle)
    (unless got
      (signal 'nl-clj-async-error
	      (list 'nl-clj-<!!
		    "channel never became ready; scheduler idle with no runnable actor left to satisfy it"
		    chan)))
    result))

(defun nl-clj-async--blocking-put-1-standalone (chan val)
  "The real, un-baked body of `nl-clj->!!'.  See that function and this\nfile's Commentary."
  (let (result got)
    (nelisp-spawn
     (let ((chan chan) (val val))
       #'(lambda nil
	   (let
	       (cps-current-state-212 cps-current-value-211
				      cps-state-terminal-213
				      cps-state-atom-214
				      cps-binding-cps-argument-215-216
				      cps-state-atom-217
				      cps-state-let*-218
				      cps-binding-cps-argument-219-220
				      cps-state-atom-221
				      cps-state-let*-222
				      cps-binding-cps-argument-223-224
				      cps-state-atom-225
				      cps-state-let*-226
				      cps-state-atom-227
				      cps-state-atom-229
				      cps-state-iter-yield-230
				      cps-state-atom-231
				      cps-state-atom-232
				      cps-state-while-228
				      cps-state-atom-233)
	     (setq cps-state-terminal-213
		   #'(lambda nil
		       (signal 'iter-end-of-sequence
			       cps-current-value-211)))
	     (setq cps-state-atom-214
		   #'(lambda nil
		       (setq cps-current-value-211
			     (prog1 (progn (setq got t))
			       (setq cps-current-state-212
				     cps-state-terminal-213)))))
	     (setq cps-state-atom-217
		   #'(lambda nil
		       (setq cps-current-value-211
			     (prog1
				 (let* nil
				   (setq result
					 cps-binding-cps-argument-215-216))
			       (setq cps-current-state-212
				     cps-state-atom-214)))))
	     (setq cps-state-let*-218
		   #'(lambda nil
		       (progn
			 (setq cps-binding-cps-argument-215-216
			       cps-current-value-211)
			 (setq cps-current-state-212
			       cps-state-atom-217))))
	     (setq cps-state-atom-221
		   #'(lambda nil
		       (setq cps-current-value-211
			     (prog1
				 (let* nil
				   (not
				    cps-binding-cps-argument-219-220))
			       (setq cps-current-state-212
				     cps-state-let*-218)))))
	     (setq cps-state-let*-222
		   #'(lambda nil
		       (progn
			 (setq cps-binding-cps-argument-219-220
			       cps-current-value-211)
			 (setq cps-current-state-212
			       cps-state-atom-221))))
	     (setq cps-state-atom-225
		   #'(lambda nil
		       (setq cps-current-value-211
			     (prog1
				 (let* nil
				   (eq
				    cps-binding-cps-argument-223-224
				    :error-closed))
			       (setq cps-current-state-212
				     cps-state-let*-222)))))
	     (setq cps-state-let*-226
		   #'(lambda nil
		       (progn
			 (setq cps-binding-cps-argument-223-224
			       cps-current-value-211)
			 (setq cps-current-state-212
			       cps-state-atom-225))))
	     (setq cps-state-atom-227
		   #'(lambda nil
		       (setq cps-current-value-211
			     (prog1
				 (progn
				   (nelisp-actor--mailbox-pop
				    nelisp-actor--current))
			       (setq cps-current-state-212
				     cps-state-let*-226)))))
	     (setq cps-state-atom-229
		   #'(lambda nil
		       (setq cps-current-value-211
			     (prog1
				 (null
				  (nelisp-actor-mailbox
				   nelisp-actor--current))
			       (setq cps-current-state-212
				     cps-state-while-228)))))
	     (setq cps-state-iter-yield-230
		   #'(lambda nil
		       (progn
			 (setq cps-current-state-212
			       cps-state-atom-229)
			 (throw 'cps--yield cps-current-value-211))))
	     (setq cps-state-atom-231
		   #'(lambda nil
		       (setq cps-current-value-211
			     (prog1 'nelisp-actor--receive
			       (setq cps-current-state-212
				     cps-state-iter-yield-230)))))
	     (setq cps-state-atom-232
		   #'(lambda nil
		       (setq cps-current-value-211
			     (prog1
				 (let* ((v nelisp-actor--current))
				   (nelisp-actor--set-status v
							     :blocked-receive))
			       (setq cps-current-state-212
				     cps-state-atom-231)))))
	     (setq cps-state-while-228
		   #'(lambda nil
		       (progn
			 (setq cps-current-state-212
			       (if cps-current-value-211
				   cps-state-atom-232
				 cps-state-atom-227)))))
	     (setq cps-state-atom-233
		   #'(lambda nil
		       (setq cps-current-value-211
			     (prog1
				 (nelisp-send (nelisp-chan-actor chan)
					      (list :send val
						    (nelisp-self)))
			       (setq cps-current-state-212
				     cps-state-atom-229)))))
	     (setq cps-current-state-212 cps-state-atom-233)
	     (let
		 ((iterator
		   #'(lambda (op value)
		       (cond
			((eq op :close)
			 (progn
			   (setq cps-current-state-212
				 cps-state-terminal-213)
			   (setq cps-current-value-211 nil)))
			((eq op :next)
			 (setq cps-current-value-211 value)
			 (let ((yielded nil))
			   (unwind-protect
			       (prog1
				   (catch 'cps--yield
				     (while t
				       (funcall cps-current-state-212)))
				 (setq yielded t))
			     (if yielded nil
			       (progn
				 (setq cps-current-state-212
				       cps-state-terminal-213)
				 (setq cps-current-value-211 nil))))))
			(t (error "Unknown iterator operation %S" op))))))
	       nil iterator)))))
    (nelisp-actor-run-until-idle)
    (unless got
      (signal 'nl-clj-async-error
	      (list 'nl-clj->!!
		    "channel put never completed; scheduler idle with no runnable actor left to satisfy it"
		    chan val)))
    result))

(defun nl-clj-async-demo-ping-pong-standalone (hops)
  "Bounce a counter between two `nl-clj-go' blocks, each talking over\nits own channel, for HOPS round trips.  HOPS must be >= 1 -- ping's\nown initial send has no unconditional guard around it (see this file's\nCommentary: wrapping even a single, one-shot parking call in a `when'\nhere, to special-case HOPS = 0, was tried and itself measured to hang\nstandalone, so it stays unconditional and HOPS = 0 is simply not a\nsupported input, an accepted, narrow gap rather than a worked-around\none).  Returns a plist:\n\n  :trail        ((ACTOR . N) ...) in the order each hop was received --\n                same shape as `nelisp-demo-ping-pong''s own :trail\n  :ping-result  the ping go-block's own return value (`:ping-done')\n  :pong-result  likewise for pong (`:pong-done')\n\nBoth sides terminate cleanly (unlike the raw-actor demo this mirrors,\nwhich deliberately leaves ping dangling for every HOPS value): pong\nalways replies exactly HOPS times; ping sends the initial value plus\nexactly (HOPS - 1) replies, so the last exchange is pong's alone --\nsymmetric, counter-bounded termination on both sides, no `close!'\nneeded to unpark a final dangling receive."
  (nelisp-actor--reset)
  (let
      ((to-ping (nl-clj-chan)) (to-pong (nl-clj-chan)) (trail nil)
       pong-chan ping-chan)
    (setq pong-chan
	  (let ((nl-clj-go-chan234 (nl-clj-chan 1)))
	    (nelisp-spawn
	     #'(lambda nil
		 (let
		     (cps-current-state-236 cps-current-value-235
					    cps-state-terminal-237
					    cps-state-atom-238
					    cps-binding---ack---239
					    cps-state-atom-240
					    cps-state-let*-241
					    cps-state-atom-242
					    cps-state-atom-244
					    cps-state-iter-yield-245
					    cps-state-atom-246
					    cps-state-atom-247
					    cps-state-while-243
					    cps-state-atom-248
					    cps-binding-i-249
					    cps-state-atom-251
					    cps-state-atom-252
					    cps-binding-n-253
					    cps-binding---ack---254
					    cps-state-atom-255
					    cps-state-let*-256
					    cps-state-atom-257
					    cps-state-atom-259
					    cps-state-iter-yield-260
					    cps-state-atom-261
					    cps-state-atom-262
					    cps-state-while-258
					    cps-state-atom-263
					    cps-state-atom-264
					    cps-state-let*-265
					    cps-binding-cps-argument-266-267
					    cps-state-atom-268
					    cps-state-let*-269
					    cps-binding-cps-argument-270-271
					    cps-state-atom-272
					    cps-state-let*-273
					    cps-state-atom-274
					    cps-state-atom-276
					    cps-state-iter-yield-277
					    cps-state-atom-278
					    cps-state-atom-279
					    cps-state-while-275
					    cps-state-atom-280
					    cps-state-while-250
					    cps-state-let*-281
					    cps-state-atom-282)
		   (setq cps-state-terminal-237
			 #'(lambda nil
			     (signal 'iter-end-of-sequence
				     cps-current-value-235)))
		   (setq cps-state-atom-238
			 #'(lambda nil
			     (setq cps-current-value-235
				   (prog1
				       (progn
					 (nelisp-chan-close
					  nl-clj-go-chan234))
				     (setq cps-current-state-236
					   cps-state-terminal-237)))))
		   (setq cps-state-atom-240
			 #'(lambda nil
			     (setq cps-current-value-235
				   (prog1
				       (let* nil
					 (cond
					  ((eq cps-binding---ack---239
					       :ok)
					   nil)
					  ((eq cps-binding---ack---239
					       :error-closed)
					   (signal 'nelisp-actor-error
						   (list
						    'chan-send-on-closed)))
					  (t
					   (signal 'nelisp-actor-error
						   (list
						    'chan-send-unexpected
						    cps-binding---ack---239)))))
				     (setq cps-current-state-236
					   cps-state-atom-238)))))
		   (setq cps-state-let*-241
			 #'(lambda nil
			     (progn
			       (setq cps-binding---ack---239
				     cps-current-value-235)
			       (setq cps-current-state-236
				     cps-state-atom-240))))
		   (setq cps-state-atom-242
			 #'(lambda nil
			     (setq cps-current-value-235
				   (prog1
				       (progn
					 (nelisp-actor--mailbox-pop
					  nelisp-actor--current))
				     (setq cps-current-state-236
					   cps-state-let*-241)))))
		   (setq cps-state-atom-244
			 #'(lambda nil
			     (setq cps-current-value-235
				   (prog1
				       (null
					(nelisp-actor-mailbox
					 nelisp-actor--current))
				     (setq cps-current-state-236
					   cps-state-while-243)))))
		   (setq cps-state-iter-yield-245
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-236
				     cps-state-atom-244)
			       (throw 'cps--yield
				      cps-current-value-235))))
		   (setq cps-state-atom-246
			 #'(lambda nil
			     (setq cps-current-value-235
				   (prog1 'nelisp-actor--receive
				     (setq cps-current-state-236
					   cps-state-iter-yield-245)))))
		   (setq cps-state-atom-247
			 #'(lambda nil
			     (setq cps-current-value-235
				   (prog1
				       (let*
					   ((v nelisp-actor--current))
					 (nelisp-actor--set-status v
								   :blocked-receive))
				     (setq cps-current-state-236
					   cps-state-atom-246)))))
		   (setq cps-state-while-243
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-236
				     (if cps-current-value-235
					 cps-state-atom-247
				       cps-state-atom-242)))))
		   (setq cps-state-atom-248
			 #'(lambda nil
			     (setq cps-current-value-235
				   (prog1
				       (nelisp-send
					(nelisp-chan-actor
					 nl-clj-go-chan234)
					(list :send :pong-done
					      (nelisp-self)))
				     (setq cps-current-state-236
					   cps-state-atom-244)))))
		   (setq cps-state-atom-251
			 #'(lambda nil
			     (setq cps-current-value-235
				   (prog1 (< cps-binding-i-249 hops)
				     (setq cps-current-state-236
					   cps-state-while-250)))))
		   (setq cps-state-atom-252
			 #'(lambda nil
			     (setq cps-current-value-235
				   (prog1
				       (progn
					 (setq cps-binding-i-249
					       (1+ cps-binding-i-249)))
				     (setq cps-current-state-236
					   cps-state-atom-251)))))
		   (setq cps-state-atom-255
			 #'(lambda nil
			     (setq cps-current-value-235
				   (prog1
				       (let* nil
					 (cond
					  ((eq cps-binding---ack---254
					       :ok)
					   nil)
					  ((eq cps-binding---ack---254
					       :error-closed)
					   (signal 'nelisp-actor-error
						   (list
						    'chan-send-on-closed)))
					  (t
					   (signal 'nelisp-actor-error
						   (list
						    'chan-send-unexpected
						    cps-binding---ack---254)))))
				     (setq cps-current-state-236
					   cps-state-atom-252)))))
		   (setq cps-state-let*-256
			 #'(lambda nil
			     (progn
			       (setq cps-binding---ack---254
				     cps-current-value-235)
			       (setq cps-current-state-236
				     cps-state-atom-255))))
		   (setq cps-state-atom-257
			 #'(lambda nil
			     (setq cps-current-value-235
				   (prog1
				       (progn
					 (nelisp-actor--mailbox-pop
					  nelisp-actor--current))
				     (setq cps-current-state-236
					   cps-state-let*-256)))))
		   (setq cps-state-atom-259
			 #'(lambda nil
			     (setq cps-current-value-235
				   (prog1
				       (null
					(nelisp-actor-mailbox
					 nelisp-actor--current))
				     (setq cps-current-state-236
					   cps-state-while-258)))))
		   (setq cps-state-iter-yield-260
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-236
				     cps-state-atom-259)
			       (throw 'cps--yield
				      cps-current-value-235))))
		   (setq cps-state-atom-261
			 #'(lambda nil
			     (setq cps-current-value-235
				   (prog1 'nelisp-actor--receive
				     (setq cps-current-state-236
					   cps-state-iter-yield-260)))))
		   (setq cps-state-atom-262
			 #'(lambda nil
			     (setq cps-current-value-235
				   (prog1
				       (let*
					   ((v nelisp-actor--current))
					 (nelisp-actor--set-status v
								   :blocked-receive))
				     (setq cps-current-state-236
					   cps-state-atom-261)))))
		   (setq cps-state-while-258
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-236
				     (if cps-current-value-235
					 cps-state-atom-262
				       cps-state-atom-257)))))
		   (setq cps-state-atom-263
			 #'(lambda nil
			     (setq cps-current-value-235
				   (prog1
				       (nelisp-send
					(nelisp-chan-actor to-ping)
					(list :send
					      (1+ cps-binding-n-253)
					      (nelisp-self)))
				     (setq cps-current-state-236
					   cps-state-atom-259)))))
		   (setq cps-state-atom-264
			 #'(lambda nil
			     (setq cps-current-value-235
				   (prog1
				       (setq trail
					     (cons
					      (cons 'pong
						    cps-binding-n-253)
					      trail))
				     (setq cps-current-state-236
					   cps-state-atom-263)))))
		   (setq cps-state-let*-265
			 #'(lambda nil
			     (progn
			       (setq cps-binding-n-253
				     cps-current-value-235)
			       (setq cps-current-state-236
				     cps-state-atom-264))))
		   (setq cps-state-atom-268
			 #'(lambda nil
			     (setq cps-current-value-235
				   (prog1
				       (let* nil
					 (car
					  cps-binding-cps-argument-266-267))
				     (setq cps-current-state-236
					   cps-state-let*-265)))))
		   (setq cps-state-let*-269
			 #'(lambda nil
			     (progn
			       (setq cps-binding-cps-argument-266-267
				     cps-current-value-235)
			       (setq cps-current-state-236
				     cps-state-atom-268))))
		   (setq cps-state-atom-272
			 #'(lambda nil
			     (setq cps-current-value-235
				   (prog1
				       (let* nil
					 (cdr
					  cps-binding-cps-argument-270-271))
				     (setq cps-current-state-236
					   cps-state-let*-269)))))
		   (setq cps-state-let*-273
			 #'(lambda nil
			     (progn
			       (setq cps-binding-cps-argument-270-271
				     cps-current-value-235)
			       (setq cps-current-state-236
				     cps-state-atom-272))))
		   (setq cps-state-atom-274
			 #'(lambda nil
			     (setq cps-current-value-235
				   (prog1
				       (progn
					 (nelisp-actor--mailbox-pop
					  nelisp-actor--current))
				     (setq cps-current-state-236
					   cps-state-let*-273)))))
		   (setq cps-state-atom-276
			 #'(lambda nil
			     (setq cps-current-value-235
				   (prog1
				       (null
					(nelisp-actor-mailbox
					 nelisp-actor--current))
				     (setq cps-current-state-236
					   cps-state-while-275)))))
		   (setq cps-state-iter-yield-277
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-236
				     cps-state-atom-276)
			       (throw 'cps--yield
				      cps-current-value-235))))
		   (setq cps-state-atom-278
			 #'(lambda nil
			     (setq cps-current-value-235
				   (prog1 'nelisp-actor--receive
				     (setq cps-current-state-236
					   cps-state-iter-yield-277)))))
		   (setq cps-state-atom-279
			 #'(lambda nil
			     (setq cps-current-value-235
				   (prog1
				       (let*
					   ((v nelisp-actor--current))
					 (nelisp-actor--set-status v
								   :blocked-receive))
				     (setq cps-current-state-236
					   cps-state-atom-278)))))
		   (setq cps-state-while-275
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-236
				     (if cps-current-value-235
					 cps-state-atom-279
				       cps-state-atom-274)))))
		   (setq cps-state-atom-280
			 #'(lambda nil
			     (setq cps-current-value-235
				   (prog1
				       (nelisp-send
					(nelisp-chan-actor to-pong)
					(list :recv (nelisp-self)))
				     (setq cps-current-state-236
					   cps-state-atom-276)))))
		   (setq cps-state-while-250
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-236
				     (if cps-current-value-235
					 cps-state-atom-280
				       cps-state-atom-248)))))
		   (setq cps-state-let*-281
			 #'(lambda nil
			     (progn
			       (setq cps-binding-i-249
				     cps-current-value-235)
			       (setq cps-current-state-236
				     cps-state-atom-251))))
		   (setq cps-state-atom-282
			 #'(lambda nil
			     (setq cps-current-value-235
				   (prog1 0
				     (setq cps-current-state-236
					   cps-state-let*-281)))))
		   (setq cps-current-state-236 cps-state-atom-282)
		   (let
		       ((iterator
			 #'(lambda (op value)
			     (cond
			      ((eq op :close)
			       (progn
				 (setq cps-current-state-236
				       cps-state-terminal-237)
				 (setq cps-current-value-235 nil)))
			      ((eq op :next)
			       (setq cps-current-value-235 value)
			       (let ((yielded nil))
				 (unwind-protect
				     (prog1
					 (catch 'cps--yield
					   (while t
					     (funcall
					      cps-current-state-236)))
				       (setq yielded t))
				   (if yielded nil
				     (progn
				       (setq cps-current-state-236
					     cps-state-terminal-237)
				       (setq cps-current-value-235 nil))))))
			      (t
			       (error "Unknown iterator operation %S"
				      op))))))
		     nil iterator))))
	    nl-clj-go-chan234))
    (setq ping-chan
	  (let ((nl-clj-go-chan283 (nl-clj-chan 1)))
	    (nelisp-spawn
	     #'(lambda nil
		 (let
		     (cps-current-state-285 cps-current-value-284
					    cps-state-terminal-286
					    cps-state-atom-287
					    cps-binding---ack---288
					    cps-state-atom-289
					    cps-state-let*-290
					    cps-state-atom-291
					    cps-state-atom-293
					    cps-state-iter-yield-294
					    cps-state-atom-295
					    cps-state-atom-296
					    cps-state-while-292
					    cps-state-atom-297
					    cps-binding-i-298
					    cps-state-atom-300
					    cps-state-atom-301
					    cps-binding-n-302
					    cps-binding---ack---303
					    cps-state-atom-304
					    cps-state-let*-305
					    cps-state-atom-306
					    cps-state-atom-308
					    cps-state-iter-yield-309
					    cps-state-atom-310
					    cps-state-atom-311
					    cps-state-while-307
					    cps-state-atom-312
					    cps-state-atom-313
					    cps-state-if-314
					    cps-state-atom-315
					    cps-state-atom-316
					    cps-state-let*-317
					    cps-binding-cps-argument-318-319
					    cps-state-atom-320
					    cps-state-let*-321
					    cps-binding-cps-argument-322-323
					    cps-state-atom-324
					    cps-state-let*-325
					    cps-state-atom-326
					    cps-state-atom-328
					    cps-state-iter-yield-329
					    cps-state-atom-330
					    cps-state-atom-331
					    cps-state-while-327
					    cps-state-atom-332
					    cps-state-while-299
					    cps-state-let*-333
					    cps-state-atom-334
					    cps-binding---ack---335
					    cps-state-atom-336
					    cps-state-let*-337
					    cps-state-atom-338
					    cps-state-atom-340
					    cps-state-iter-yield-341
					    cps-state-atom-342
					    cps-state-atom-343
					    cps-state-while-339
					    cps-state-atom-344)
		   (setq cps-state-terminal-286
			 #'(lambda nil
			     (signal 'iter-end-of-sequence
				     cps-current-value-284)))
		   (setq cps-state-atom-287
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1
				       (progn
					 (nelisp-chan-close
					  nl-clj-go-chan283))
				     (setq cps-current-state-285
					   cps-state-terminal-286)))))
		   (setq cps-state-atom-289
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1
				       (let* nil
					 (cond
					  ((eq cps-binding---ack---288
					       :ok)
					   nil)
					  ((eq cps-binding---ack---288
					       :error-closed)
					   (signal 'nelisp-actor-error
						   (list
						    'chan-send-on-closed)))
					  (t
					   (signal 'nelisp-actor-error
						   (list
						    'chan-send-unexpected
						    cps-binding---ack---288)))))
				     (setq cps-current-state-285
					   cps-state-atom-287)))))
		   (setq cps-state-let*-290
			 #'(lambda nil
			     (progn
			       (setq cps-binding---ack---288
				     cps-current-value-284)
			       (setq cps-current-state-285
				     cps-state-atom-289))))
		   (setq cps-state-atom-291
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1
				       (progn
					 (nelisp-actor--mailbox-pop
					  nelisp-actor--current))
				     (setq cps-current-state-285
					   cps-state-let*-290)))))
		   (setq cps-state-atom-293
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1
				       (null
					(nelisp-actor-mailbox
					 nelisp-actor--current))
				     (setq cps-current-state-285
					   cps-state-while-292)))))
		   (setq cps-state-iter-yield-294
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-285
				     cps-state-atom-293)
			       (throw 'cps--yield
				      cps-current-value-284))))
		   (setq cps-state-atom-295
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1 'nelisp-actor--receive
				     (setq cps-current-state-285
					   cps-state-iter-yield-294)))))
		   (setq cps-state-atom-296
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1
				       (let*
					   ((v nelisp-actor--current))
					 (nelisp-actor--set-status v
								   :blocked-receive))
				     (setq cps-current-state-285
					   cps-state-atom-295)))))
		   (setq cps-state-while-292
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-285
				     (if cps-current-value-284
					 cps-state-atom-296
				       cps-state-atom-291)))))
		   (setq cps-state-atom-297
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1
				       (nelisp-send
					(nelisp-chan-actor
					 nl-clj-go-chan283)
					(list :send :ping-done
					      (nelisp-self)))
				     (setq cps-current-state-285
					   cps-state-atom-293)))))
		   (setq cps-state-atom-300
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1 (< cps-binding-i-298 hops)
				     (setq cps-current-state-285
					   cps-state-while-299)))))
		   (setq cps-state-atom-301
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1
				       (progn
					 (setq cps-binding-i-298
					       (1+ cps-binding-i-298)))
				     (setq cps-current-state-285
					   cps-state-atom-300)))))
		   (setq cps-state-atom-304
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1
				       (let* nil
					 (cond
					  ((eq cps-binding---ack---303
					       :ok)
					   nil)
					  ((eq cps-binding---ack---303
					       :error-closed)
					   (signal 'nelisp-actor-error
						   (list
						    'chan-send-on-closed)))
					  (t
					   (signal 'nelisp-actor-error
						   (list
						    'chan-send-unexpected
						    cps-binding---ack---303)))))
				     (setq cps-current-state-285
					   cps-state-atom-301)))))
		   (setq cps-state-let*-305
			 #'(lambda nil
			     (progn
			       (setq cps-binding---ack---303
				     cps-current-value-284)
			       (setq cps-current-state-285
				     cps-state-atom-304))))
		   (setq cps-state-atom-306
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1
				       (progn
					 (nelisp-actor--mailbox-pop
					  nelisp-actor--current))
				     (setq cps-current-state-285
					   cps-state-let*-305)))))
		   (setq cps-state-atom-308
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1
				       (null
					(nelisp-actor-mailbox
					 nelisp-actor--current))
				     (setq cps-current-state-285
					   cps-state-while-307)))))
		   (setq cps-state-iter-yield-309
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-285
				     cps-state-atom-308)
			       (throw 'cps--yield
				      cps-current-value-284))))
		   (setq cps-state-atom-310
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1 'nelisp-actor--receive
				     (setq cps-current-state-285
					   cps-state-iter-yield-309)))))
		   (setq cps-state-atom-311
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1
				       (let*
					   ((v nelisp-actor--current))
					 (nelisp-actor--set-status v
								   :blocked-receive))
				     (setq cps-current-state-285
					   cps-state-atom-310)))))
		   (setq cps-state-while-307
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-285
				     (if cps-current-value-284
					 cps-state-atom-311
				       cps-state-atom-306)))))
		   (setq cps-state-atom-312
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1
				       (nelisp-send
					(nelisp-chan-actor to-pong)
					(list :send
					      (1+ cps-binding-n-302)
					      (nelisp-self)))
				     (setq cps-current-state-285
					   cps-state-atom-308)))))
		   (setq cps-state-atom-313
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1 (progn)
				     (setq cps-current-state-285
					   cps-state-atom-301)))))
		   (setq cps-state-if-314
			 #'(lambda nil
			     (setq cps-current-state-285
				   (if cps-current-value-284
				       cps-state-atom-312
				     cps-state-atom-313))))
		   (setq cps-state-atom-315
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1
				       (< (1+ cps-binding-i-298) hops)
				     (setq cps-current-state-285
					   cps-state-if-314)))))
		   (setq cps-state-atom-316
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1
				       (setq trail
					     (cons
					      (cons 'ping
						    cps-binding-n-302)
					      trail))
				     (setq cps-current-state-285
					   cps-state-atom-315)))))
		   (setq cps-state-let*-317
			 #'(lambda nil
			     (progn
			       (setq cps-binding-n-302
				     cps-current-value-284)
			       (setq cps-current-state-285
				     cps-state-atom-316))))
		   (setq cps-state-atom-320
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1
				       (let* nil
					 (car
					  cps-binding-cps-argument-318-319))
				     (setq cps-current-state-285
					   cps-state-let*-317)))))
		   (setq cps-state-let*-321
			 #'(lambda nil
			     (progn
			       (setq cps-binding-cps-argument-318-319
				     cps-current-value-284)
			       (setq cps-current-state-285
				     cps-state-atom-320))))
		   (setq cps-state-atom-324
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1
				       (let* nil
					 (cdr
					  cps-binding-cps-argument-322-323))
				     (setq cps-current-state-285
					   cps-state-let*-321)))))
		   (setq cps-state-let*-325
			 #'(lambda nil
			     (progn
			       (setq cps-binding-cps-argument-322-323
				     cps-current-value-284)
			       (setq cps-current-state-285
				     cps-state-atom-324))))
		   (setq cps-state-atom-326
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1
				       (progn
					 (nelisp-actor--mailbox-pop
					  nelisp-actor--current))
				     (setq cps-current-state-285
					   cps-state-let*-325)))))
		   (setq cps-state-atom-328
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1
				       (null
					(nelisp-actor-mailbox
					 nelisp-actor--current))
				     (setq cps-current-state-285
					   cps-state-while-327)))))
		   (setq cps-state-iter-yield-329
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-285
				     cps-state-atom-328)
			       (throw 'cps--yield
				      cps-current-value-284))))
		   (setq cps-state-atom-330
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1 'nelisp-actor--receive
				     (setq cps-current-state-285
					   cps-state-iter-yield-329)))))
		   (setq cps-state-atom-331
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1
				       (let*
					   ((v nelisp-actor--current))
					 (nelisp-actor--set-status v
								   :blocked-receive))
				     (setq cps-current-state-285
					   cps-state-atom-330)))))
		   (setq cps-state-while-327
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-285
				     (if cps-current-value-284
					 cps-state-atom-331
				       cps-state-atom-326)))))
		   (setq cps-state-atom-332
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1
				       (nelisp-send
					(nelisp-chan-actor to-ping)
					(list :recv (nelisp-self)))
				     (setq cps-current-state-285
					   cps-state-atom-328)))))
		   (setq cps-state-while-299
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-285
				     (if cps-current-value-284
					 cps-state-atom-332
				       cps-state-atom-297)))))
		   (setq cps-state-let*-333
			 #'(lambda nil
			     (progn
			       (setq cps-binding-i-298
				     cps-current-value-284)
			       (setq cps-current-state-285
				     cps-state-atom-300))))
		   (setq cps-state-atom-334
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1 0
				     (setq cps-current-state-285
					   cps-state-let*-333)))))
		   (setq cps-state-atom-336
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1
				       (let* nil
					 (cond
					  ((eq cps-binding---ack---335
					       :ok)
					   nil)
					  ((eq cps-binding---ack---335
					       :error-closed)
					   (signal 'nelisp-actor-error
						   (list
						    'chan-send-on-closed)))
					  (t
					   (signal 'nelisp-actor-error
						   (list
						    'chan-send-unexpected
						    cps-binding---ack---335)))))
				     (setq cps-current-state-285
					   cps-state-atom-334)))))
		   (setq cps-state-let*-337
			 #'(lambda nil
			     (progn
			       (setq cps-binding---ack---335
				     cps-current-value-284)
			       (setq cps-current-state-285
				     cps-state-atom-336))))
		   (setq cps-state-atom-338
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1
				       (progn
					 (nelisp-actor--mailbox-pop
					  nelisp-actor--current))
				     (setq cps-current-state-285
					   cps-state-let*-337)))))
		   (setq cps-state-atom-340
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1
				       (null
					(nelisp-actor-mailbox
					 nelisp-actor--current))
				     (setq cps-current-state-285
					   cps-state-while-339)))))
		   (setq cps-state-iter-yield-341
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-285
				     cps-state-atom-340)
			       (throw 'cps--yield
				      cps-current-value-284))))
		   (setq cps-state-atom-342
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1 'nelisp-actor--receive
				     (setq cps-current-state-285
					   cps-state-iter-yield-341)))))
		   (setq cps-state-atom-343
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1
				       (let*
					   ((v nelisp-actor--current))
					 (nelisp-actor--set-status v
								   :blocked-receive))
				     (setq cps-current-state-285
					   cps-state-atom-342)))))
		   (setq cps-state-while-339
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-285
				     (if cps-current-value-284
					 cps-state-atom-343
				       cps-state-atom-338)))))
		   (setq cps-state-atom-344
			 #'(lambda nil
			     (setq cps-current-value-284
				   (prog1
				       (nelisp-send
					(nelisp-chan-actor to-pong)
					(list :send 0 (nelisp-self)))
				     (setq cps-current-state-285
					   cps-state-atom-340)))))
		   (setq cps-current-state-285 cps-state-atom-344)
		   (let
		       ((iterator
			 #'(lambda (op value)
			     (cond
			      ((eq op :close)
			       (progn
				 (setq cps-current-state-285
				       cps-state-terminal-286)
				 (setq cps-current-value-284 nil)))
			      ((eq op :next)
			       (setq cps-current-value-284 value)
			       (let ((yielded nil))
				 (unwind-protect
				     (prog1
					 (catch 'cps--yield
					   (while t
					     (funcall
					      cps-current-state-285)))
				       (setq yielded t))
				   (if yielded nil
				     (progn
				       (setq cps-current-state-285
					     cps-state-terminal-286)
				       (setq cps-current-value-284 nil))))))
			      (t
			       (error "Unknown iterator operation %S"
				      op))))))
		     nil iterator))))
	    nl-clj-go-chan283))
    (nelisp-actor-run-until-idle)
    (list :trail (nreverse trail) :ping-result (nl-clj-<!! ping-chan)
	  :pong-result (nl-clj-<!! pong-chan))))

(setq nl-clj-async--chan-ctor-standalone
      #'nl-clj-async--make-chan-1-standalone)
(setq nl-clj-async--blocking-take-standalone
      #'nl-clj-async--blocking-take-1-standalone)
(setq nl-clj-async--blocking-put-standalone
      #'nl-clj-async--blocking-put-1-standalone)

(provide 'go-ping-pong-cps)

;;; go-ping-pong-cps.el ends here
