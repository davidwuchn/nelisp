;; substrate-parity accepted divergences -- generated, review before commit.
;; Regenerate with NELISP_SUBSTRATE_PARITY_WRITE=1 (see the make
;; target); do not hand-add keys to silence a finding.
;;
;; Each key is "INDEX\tSUBSTRATE\tBASELINE\tACTUAL" -- both sides
;; of the divergence, not just its existence.  If either value
;; changes the key stops matching, the old entry goes stale, and
;; the new shape comes back as an unaccepted finding instead of
;; being silently covered (the ns-gate lesson).
;;
;; :notes is an alist of key -> why THIS entry is accepted, and it
;; survives regeneration.
(:generated-at "2026-08-22" :reason "Regenerated from a reviewed tree." :notes (("0	source-fallback	1	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("1	source-fallback	1	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("10	source-fallback	1	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("11	artifact	0	1" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): the bare-file substrate's `mod'/`%%' compute a C-style truncating remainder (sign follows the dividend) instead of Elisp's floor-based `mod' (sign follows the divisor), and `floor'/`truncate' silently ignore a 2-argument divisor and return the dividend unchanged -- a silent wrong answer, not a crash. Confirmed by direct exit-code probes outside this gate: bare `(mod 7 -3)' returns 1, not -2; bare `(floor 7 2)' returns 7, not 3. Every other substrate, including host Emacs, agrees and is correct. Not fixed here: the wrong dispatch lives in the bare-mode AOT driver's low-level arithmetic table (scripts/nelisp-standalone-build.el) and a mechanical fix deserves its own targeted before/after measurement rather than a patch guessed at from this gate.") ("11	host	0	1" . "GAP vs host Emacs, consistent across every NeLisp substrate that can run this form -- not a cross-substrate NeLisp divergence. Form 20 (`emacs-pid') in particular is void-function in EVERY NeLisp substrate alike (bare, --load, runtime-image, artifact, source-cache), so it is a missing-primitive backlog item versus host Emacs, not a parity defect between NeLisp entry points.") ("11	load	0	1" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): the bare-file substrate's `mod'/`%%' compute a C-style truncating remainder (sign follows the dividend) instead of Elisp's floor-based `mod' (sign follows the divisor), and `floor'/`truncate' silently ignore a 2-argument divisor and return the dividend unchanged -- a silent wrong answer, not a crash. Confirmed by direct exit-code probes outside this gate: bare `(mod 7 -3)' returns 1, not -2; bare `(floor 7 2)' returns 7, not 3. Every other substrate, including host Emacs, agrees and is correct. Not fixed here: the wrong dispatch lives in the bare-mode AOT driver's low-level arithmetic table (scripts/nelisp-standalone-build.el) and a mechanical fix deserves its own targeted before/after measurement rather than a patch guessed at from this gate.") ("11	runtime-image	0	1" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): the bare-file substrate's `mod'/`%%' compute a C-style truncating remainder (sign follows the dividend) instead of Elisp's floor-based `mod' (sign follows the divisor), and `floor'/`truncate' silently ignore a 2-argument divisor and return the dividend unchanged -- a silent wrong answer, not a crash. Confirmed by direct exit-code probes outside this gate: bare `(mod 7 -3)' returns 1, not -2; bare `(floor 7 2)' returns 7, not 3. Every other substrate, including host Emacs, agrees and is correct. Not fixed here: the wrong dispatch lives in the bare-mode AOT driver's low-level arithmetic table (scripts/nelisp-standalone-build.el) and a mechanical fix deserves its own targeted before/after measurement rather than a patch guessed at from this gate.") ("11	source-cache	0	1" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): the bare-file substrate's `mod'/`%%' compute a C-style truncating remainder (sign follows the dividend) instead of Elisp's floor-based `mod' (sign follows the divisor), and `floor'/`truncate' silently ignore a 2-argument divisor and return the dividend unchanged -- a silent wrong answer, not a crash. Confirmed by direct exit-code probes outside this gate: bare `(mod 7 -3)' returns 1, not -2; bare `(floor 7 2)' returns 7, not 3. Every other substrate, including host Emacs, agrees and is correct. Not fixed here: the wrong dispatch lives in the bare-mode AOT driver's low-level arithmetic table (scripts/nelisp-standalone-build.el) and a mechanical fix deserves its own targeted before/after measurement rather than a patch guessed at from this gate.") ("11	source-fallback	0	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("12	source-fallback	1	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("13	artifact	0	1" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): the bare-file substrate's `mod'/`%%' compute a C-style truncating remainder (sign follows the dividend) instead of Elisp's floor-based `mod' (sign follows the divisor), and `floor'/`truncate' silently ignore a 2-argument divisor and return the dividend unchanged -- a silent wrong answer, not a crash. Confirmed by direct exit-code probes outside this gate: bare `(mod 7 -3)' returns 1, not -2; bare `(floor 7 2)' returns 7, not 3. Every other substrate, including host Emacs, agrees and is correct. Not fixed here: the wrong dispatch lives in the bare-mode AOT driver's low-level arithmetic table (scripts/nelisp-standalone-build.el) and a mechanical fix deserves its own targeted before/after measurement rather than a patch guessed at from this gate.") ("13	host	0	1" . "GAP vs host Emacs, consistent across every NeLisp substrate that can run this form -- not a cross-substrate NeLisp divergence. Form 20 (`emacs-pid') in particular is void-function in EVERY NeLisp substrate alike (bare, --load, runtime-image, artifact, source-cache), so it is a missing-primitive backlog item versus host Emacs, not a parity defect between NeLisp entry points.") ("13	load	0	1" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): the bare-file substrate's `mod'/`%%' compute a C-style truncating remainder (sign follows the dividend) instead of Elisp's floor-based `mod' (sign follows the divisor), and `floor'/`truncate' silently ignore a 2-argument divisor and return the dividend unchanged -- a silent wrong answer, not a crash. Confirmed by direct exit-code probes outside this gate: bare `(mod 7 -3)' returns 1, not -2; bare `(floor 7 2)' returns 7, not 3. Every other substrate, including host Emacs, agrees and is correct. Not fixed here: the wrong dispatch lives in the bare-mode AOT driver's low-level arithmetic table (scripts/nelisp-standalone-build.el) and a mechanical fix deserves its own targeted before/after measurement rather than a patch guessed at from this gate.") ("13	runtime-image	0	1" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): the bare-file substrate's `mod'/`%%' compute a C-style truncating remainder (sign follows the dividend) instead of Elisp's floor-based `mod' (sign follows the divisor), and `floor'/`truncate' silently ignore a 2-argument divisor and return the dividend unchanged -- a silent wrong answer, not a crash. Confirmed by direct exit-code probes outside this gate: bare `(mod 7 -3)' returns 1, not -2; bare `(floor 7 2)' returns 7, not 3. Every other substrate, including host Emacs, agrees and is correct. Not fixed here: the wrong dispatch lives in the bare-mode AOT driver's low-level arithmetic table (scripts/nelisp-standalone-build.el) and a mechanical fix deserves its own targeted before/after measurement rather than a patch guessed at from this gate.") ("13	source-cache	0	1" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): the bare-file substrate's `mod'/`%%' compute a C-style truncating remainder (sign follows the dividend) instead of Elisp's floor-based `mod' (sign follows the divisor), and `floor'/`truncate' silently ignore a 2-argument divisor and return the dividend unchanged -- a silent wrong answer, not a crash. Confirmed by direct exit-code probes outside this gate: bare `(mod 7 -3)' returns 1, not -2; bare `(floor 7 2)' returns 7, not 3. Every other substrate, including host Emacs, agrees and is correct. Not fixed here: the wrong dispatch lives in the bare-mode AOT driver's low-level arithmetic table (scripts/nelisp-standalone-build.el) and a mechanical fix deserves its own targeted before/after measurement rather than a patch guessed at from this gate.") ("13	source-fallback	0	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("14	artifact	0	1" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): the bare-file substrate's `mod'/`%%' compute a C-style truncating remainder (sign follows the dividend) instead of Elisp's floor-based `mod' (sign follows the divisor), and `floor'/`truncate' silently ignore a 2-argument divisor and return the dividend unchanged -- a silent wrong answer, not a crash. Confirmed by direct exit-code probes outside this gate: bare `(mod 7 -3)' returns 1, not -2; bare `(floor 7 2)' returns 7, not 3. Every other substrate, including host Emacs, agrees and is correct. Not fixed here: the wrong dispatch lives in the bare-mode AOT driver's low-level arithmetic table (scripts/nelisp-standalone-build.el) and a mechanical fix deserves its own targeted before/after measurement rather than a patch guessed at from this gate.") ("14	host	0	1" . "GAP vs host Emacs, consistent across every NeLisp substrate that can run this form -- not a cross-substrate NeLisp divergence. Form 20 (`emacs-pid') in particular is void-function in EVERY NeLisp substrate alike (bare, --load, runtime-image, artifact, source-cache), so it is a missing-primitive backlog item versus host Emacs, not a parity defect between NeLisp entry points.") ("14	load	0	1" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): the bare-file substrate's `mod'/`%%' compute a C-style truncating remainder (sign follows the dividend) instead of Elisp's floor-based `mod' (sign follows the divisor), and `floor'/`truncate' silently ignore a 2-argument divisor and return the dividend unchanged -- a silent wrong answer, not a crash. Confirmed by direct exit-code probes outside this gate: bare `(mod 7 -3)' returns 1, not -2; bare `(floor 7 2)' returns 7, not 3. Every other substrate, including host Emacs, agrees and is correct. Not fixed here: the wrong dispatch lives in the bare-mode AOT driver's low-level arithmetic table (scripts/nelisp-standalone-build.el) and a mechanical fix deserves its own targeted before/after measurement rather than a patch guessed at from this gate.") ("14	runtime-image	0	1" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): the bare-file substrate's `mod'/`%%' compute a C-style truncating remainder (sign follows the dividend) instead of Elisp's floor-based `mod' (sign follows the divisor), and `floor'/`truncate' silently ignore a 2-argument divisor and return the dividend unchanged -- a silent wrong answer, not a crash. Confirmed by direct exit-code probes outside this gate: bare `(mod 7 -3)' returns 1, not -2; bare `(floor 7 2)' returns 7, not 3. Every other substrate, including host Emacs, agrees and is correct. Not fixed here: the wrong dispatch lives in the bare-mode AOT driver's low-level arithmetic table (scripts/nelisp-standalone-build.el) and a mechanical fix deserves its own targeted before/after measurement rather than a patch guessed at from this gate.") ("14	source-cache	0	1" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): the bare-file substrate's `mod'/`%%' compute a C-style truncating remainder (sign follows the dividend) instead of Elisp's floor-based `mod' (sign follows the divisor), and `floor'/`truncate' silently ignore a 2-argument divisor and return the dividend unchanged -- a silent wrong answer, not a crash. Confirmed by direct exit-code probes outside this gate: bare `(mod 7 -3)' returns 1, not -2; bare `(floor 7 2)' returns 7, not 3. Every other substrate, including host Emacs, agrees and is correct. Not fixed here: the wrong dispatch lives in the bare-mode AOT driver's low-level arithmetic table (scripts/nelisp-standalone-build.el) and a mechanical fix deserves its own targeted before/after measurement rather than a patch guessed at from this gate.") ("14	source-fallback	0	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("15	artifact	CRASH:void-function:string-match	0" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("15	host	CRASH:void-function:string-match	1" . "GAP vs host Emacs, consistent across every NeLisp substrate that can run this form -- not a cross-substrate NeLisp divergence. Form 20 (`emacs-pid') in particular is void-function in EVERY NeLisp substrate alike (bare, --load, runtime-image, artifact, source-cache), so it is a missing-primitive backlog item versus host Emacs, not a parity defect between NeLisp entry points.") ("15	load	CRASH:void-function:string-match	0" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("15	runtime-image	CRASH:void-function:string-match	0" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("15	source-cache	CRASH:void-function:string-match	0" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("15	source-fallback	CRASH:void-function:string-match	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("16	source-fallback	1	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("17	source-fallback	1	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("18	artifact	CRASH:void-function:nreverse	0" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("18	host	CRASH:void-function:nreverse	0" . "GAP vs host Emacs, consistent across every NeLisp substrate that can run this form -- not a cross-substrate NeLisp divergence. Form 20 (`emacs-pid') in particular is void-function in EVERY NeLisp substrate alike (bare, --load, runtime-image, artifact, source-cache), so it is a missing-primitive backlog item versus host Emacs, not a parity defect between NeLisp entry points.") ("18	load	CRASH:void-function:nreverse	0" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("18	runtime-image	CRASH:void-function:nreverse	0" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("18	source-cache	CRASH:void-function:nreverse	0" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("18	source-fallback	CRASH:void-function:nreverse	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("19	artifact	CRASH:void-function:read	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("19	host	CRASH:void-function:read	1" . "GAP vs host Emacs, consistent across every NeLisp substrate that can run this form -- not a cross-substrate NeLisp divergence. Form 20 (`emacs-pid') in particular is void-function in EVERY NeLisp substrate alike (bare, --load, runtime-image, artifact, source-cache), so it is a missing-primitive backlog item versus host Emacs, not a parity defect between NeLisp entry points.") ("19	load	CRASH:void-function:read	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("19	runtime-image	CRASH:void-function:read	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("19	source-cache	CRASH:void-function:read	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("19	source-fallback	CRASH:void-function:read	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("2	artifact	CRASH:void-function:intern-soft	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("2	host	CRASH:void-function:intern-soft	1" . "GAP vs host Emacs, consistent across every NeLisp substrate that can run this form -- not a cross-substrate NeLisp divergence. Form 20 (`emacs-pid') in particular is void-function in EVERY NeLisp substrate alike (bare, --load, runtime-image, artifact, source-cache), so it is a missing-primitive backlog item versus host Emacs, not a parity defect between NeLisp entry points.") ("2	load	CRASH:void-function:intern-soft	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("2	runtime-image	CRASH:void-function:intern-soft	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("2	source-cache	CRASH:void-function:intern-soft	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("2	source-fallback	CRASH:void-function:intern-soft	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("20	host	CRASH:void-function:emacs-pid	1" . "GAP vs host Emacs, consistent across every NeLisp substrate that can run this form -- not a cross-substrate NeLisp divergence. Form 20 (`emacs-pid') in particular is void-function in EVERY NeLisp substrate alike (bare, --load, runtime-image, artifact, source-cache), so it is a missing-primitive backlog item versus host Emacs, not a parity defect between NeLisp entry points.") ("20	source-fallback	CRASH:void-function:emacs-pid	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("21	artifact	CRASH:void-function:random	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("21	host	CRASH:void-function:random	1" . "GAP vs host Emacs, consistent across every NeLisp substrate that can run this form -- not a cross-substrate NeLisp divergence. Form 20 (`emacs-pid') in particular is void-function in EVERY NeLisp substrate alike (bare, --load, runtime-image, artifact, source-cache), so it is a missing-primitive backlog item versus host Emacs, not a parity defect between NeLisp entry points.") ("21	load	CRASH:void-function:random	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("21	runtime-image	CRASH:void-function:random	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("21	source-cache	CRASH:void-function:random	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("21	source-fallback	CRASH:void-function:random	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("22	artifact	CRASH:void-function:make-temp-name	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("22	host	CRASH:void-function:make-temp-name	1" . "GAP vs host Emacs, consistent across every NeLisp substrate that can run this form -- not a cross-substrate NeLisp divergence. Form 20 (`emacs-pid') in particular is void-function in EVERY NeLisp substrate alike (bare, --load, runtime-image, artifact, source-cache), so it is a missing-primitive backlog item versus host Emacs, not a parity defect between NeLisp entry points.") ("22	load	CRASH:void-function:make-temp-name	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("22	runtime-image	CRASH:void-function:make-temp-name	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("22	source-cache	CRASH:void-function:make-temp-name	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("22	source-fallback	CRASH:void-function:make-temp-name	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("23	source-fallback	1	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("24	artifact	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("24	host	0	1" . "GAP vs host Emacs, consistent across every NeLisp substrate that can run this form -- not a cross-substrate NeLisp divergence. Form 20 (`emacs-pid') in particular is void-function in EVERY NeLisp substrate alike (bare, --load, runtime-image, artifact, source-cache), so it is a missing-primitive backlog item versus host Emacs, not a parity defect between NeLisp entry points.") ("24	load	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("24	runtime-image	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("24	source-cache	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("24	source-fallback	0	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("25	artifact	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("25	host	0	1" . "GAP vs host Emacs, consistent across every NeLisp substrate that can run this form -- not a cross-substrate NeLisp divergence. Form 20 (`emacs-pid') in particular is void-function in EVERY NeLisp substrate alike (bare, --load, runtime-image, artifact, source-cache), so it is a missing-primitive backlog item versus host Emacs, not a parity defect between NeLisp entry points.") ("25	load	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("25	runtime-image	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("25	source-cache	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("25	source-fallback	0	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("26	artifact	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("26	host	0	1" . "GAP vs host Emacs, consistent across every NeLisp substrate that can run this form -- not a cross-substrate NeLisp divergence. Form 20 (`emacs-pid') in particular is void-function in EVERY NeLisp substrate alike (bare, --load, runtime-image, artifact, source-cache), so it is a missing-primitive backlog item versus host Emacs, not a parity defect between NeLisp entry points.") ("26	load	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("26	runtime-image	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("26	source-cache	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("26	source-fallback	0	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("27	artifact	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("27	host	0	1" . "GAP vs host Emacs, consistent across every NeLisp substrate that can run this form -- not a cross-substrate NeLisp divergence. Form 20 (`emacs-pid') in particular is void-function in EVERY NeLisp substrate alike (bare, --load, runtime-image, artifact, source-cache), so it is a missing-primitive backlog item versus host Emacs, not a parity defect between NeLisp entry points.") ("27	load	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("27	runtime-image	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("27	source-cache	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("27	source-fallback	0	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("28	artifact	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("28	host	0	1" . "GAP vs host Emacs, consistent across every NeLisp substrate that can run this form -- not a cross-substrate NeLisp divergence. Form 20 (`emacs-pid') in particular is void-function in EVERY NeLisp substrate alike (bare, --load, runtime-image, artifact, source-cache), so it is a missing-primitive backlog item versus host Emacs, not a parity defect between NeLisp entry points.") ("28	load	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("28	runtime-image	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("28	source-cache	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("28	source-fallback	0	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("29	source-fallback	1	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("3	artifact	CRASH:void-function:intern-soft	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("3	host	CRASH:void-function:intern-soft	1" . "GAP vs host Emacs, consistent across every NeLisp substrate that can run this form -- not a cross-substrate NeLisp divergence. Form 20 (`emacs-pid') in particular is void-function in EVERY NeLisp substrate alike (bare, --load, runtime-image, artifact, source-cache), so it is a missing-primitive backlog item versus host Emacs, not a parity defect between NeLisp entry points.") ("3	load	CRASH:void-function:intern-soft	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("3	runtime-image	CRASH:void-function:intern-soft	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("3	source-cache	CRASH:void-function:intern-soft	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("3	source-fallback	CRASH:void-function:intern-soft	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("30	source-fallback	1	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("31	source-fallback	1	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("32	artifact	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("32	host	0	1" . "GAP vs host Emacs, consistent across every NeLisp substrate that can run this form -- not a cross-substrate NeLisp divergence. Form 20 (`emacs-pid') in particular is void-function in EVERY NeLisp substrate alike (bare, --load, runtime-image, artifact, source-cache), so it is a missing-primitive backlog item versus host Emacs, not a parity defect between NeLisp entry points.") ("32	load	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("32	runtime-image	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("32	source-cache	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("32	source-fallback	0	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("33	artifact	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("33	host	0	1" . "GAP vs host Emacs, consistent across every NeLisp substrate that can run this form -- not a cross-substrate NeLisp divergence. Form 20 (`emacs-pid') in particular is void-function in EVERY NeLisp substrate alike (bare, --load, runtime-image, artifact, source-cache), so it is a missing-primitive backlog item versus host Emacs, not a parity defect between NeLisp entry points.") ("33	load	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("33	runtime-image	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("33	source-cache	0	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("33	source-fallback	0	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("34	source-fallback	1	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("4	artifact	CRASH:void-function:read-from-string	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("4	host	CRASH:void-function:read-from-string	1" . "GAP vs host Emacs, consistent across every NeLisp substrate that can run this form -- not a cross-substrate NeLisp divergence. Form 20 (`emacs-pid') in particular is void-function in EVERY NeLisp substrate alike (bare, --load, runtime-image, artifact, source-cache), so it is a missing-primitive backlog item versus host Emacs, not a parity defect between NeLisp entry points.") ("4	load	CRASH:void-function:read-from-string	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("4	runtime-image	CRASH:void-function:read-from-string	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("4	source-cache	CRASH:void-function:read-from-string	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("4	source-fallback	CRASH:void-function:read-from-string	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("5	artifact	CRASH:void-function:read-from-string	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("5	host	CRASH:void-function:read-from-string	1" . "GAP vs host Emacs, consistent across every NeLisp substrate that can run this form -- not a cross-substrate NeLisp divergence. Form 20 (`emacs-pid') in particular is void-function in EVERY NeLisp substrate alike (bare, --load, runtime-image, artifact, source-cache), so it is a missing-primitive backlog item versus host Emacs, not a parity defect between NeLisp entry points.") ("5	load	CRASH:void-function:read-from-string	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("5	runtime-image	CRASH:void-function:read-from-string	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("5	source-cache	CRASH:void-function:read-from-string	1" . "KNOWN DEFECT (2026-08-22): the bare positional FILE argument init path boots a smaller prelude than every other NeLisp entry point (--load, runtime-image, artifact, eval-elisp-source). Measured missing there and present everywhere else: intern-soft, read-from-string, princ, prin1, message, match-data, string-match, nreverse, read, random, make-temp-name. Extends the ADDENDA's pre-existing intern-soft finding (agent 1); this gate is what measured the full list. Deliberately not fixed here, per the ADDENDA's own scoping -- resolving it is a policy call (does bare-file mode gain a richer prelude, or does --load become the documented minimum) for whoever owns that entry point.") ("5	source-fallback	CRASH:void-function:read-from-string	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("6	source-fallback	1	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("7	source-fallback	1	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("8	host	0	1" . "ACCEPTED (by design, class ii): NeLisp strings are Rust UTF-8 internally, so a raw byte >= 128 written through `unibyte-string' round-trips through UTF-8 re-encoding -- `aref' does not return the original byte the way host Emacs's unibyte strings do. Documented in memory feedback_nelisp_standalone_utf8_raw_byte_block. Every NeLisp substrate agrees with every other NeLisp substrate here; only the comparison against host Emacs differs, which is exactly the documented limitation.") ("8	source-fallback	0	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.") ("9	artifact	0	1" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): the bare-file substrate's `mod'/`%%' compute a C-style truncating remainder (sign follows the dividend) instead of Elisp's floor-based `mod' (sign follows the divisor), and `floor'/`truncate' silently ignore a 2-argument divisor and return the dividend unchanged -- a silent wrong answer, not a crash. Confirmed by direct exit-code probes outside this gate: bare `(mod 7 -3)' returns 1, not -2; bare `(floor 7 2)' returns 7, not 3. Every other substrate, including host Emacs, agrees and is correct. Not fixed here: the wrong dispatch lives in the bare-mode AOT driver's low-level arithmetic table (scripts/nelisp-standalone-build.el) and a mechanical fix deserves its own targeted before/after measurement rather than a patch guessed at from this gate.") ("9	host	0	1" . "GAP vs host Emacs, consistent across every NeLisp substrate that can run this form -- not a cross-substrate NeLisp divergence. Form 20 (`emacs-pid') in particular is void-function in EVERY NeLisp substrate alike (bare, --load, runtime-image, artifact, source-cache), so it is a missing-primitive backlog item versus host Emacs, not a parity defect between NeLisp entry points.") ("9	load	0	1" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): the bare-file substrate's `mod'/`%%' compute a C-style truncating remainder (sign follows the dividend) instead of Elisp's floor-based `mod' (sign follows the divisor), and `floor'/`truncate' silently ignore a 2-argument divisor and return the dividend unchanged -- a silent wrong answer, not a crash. Confirmed by direct exit-code probes outside this gate: bare `(mod 7 -3)' returns 1, not -2; bare `(floor 7 2)' returns 7, not 3. Every other substrate, including host Emacs, agrees and is correct. Not fixed here: the wrong dispatch lives in the bare-mode AOT driver's low-level arithmetic table (scripts/nelisp-standalone-build.el) and a mechanical fix deserves its own targeted before/after measurement rather than a patch guessed at from this gate.") ("9	runtime-image	0	1" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): the bare-file substrate's `mod'/`%%' compute a C-style truncating remainder (sign follows the dividend) instead of Elisp's floor-based `mod' (sign follows the divisor), and `floor'/`truncate' silently ignore a 2-argument divisor and return the dividend unchanged -- a silent wrong answer, not a crash. Confirmed by direct exit-code probes outside this gate: bare `(mod 7 -3)' returns 1, not -2; bare `(floor 7 2)' returns 7, not 3. Every other substrate, including host Emacs, agrees and is correct. Not fixed here: the wrong dispatch lives in the bare-mode AOT driver's low-level arithmetic table (scripts/nelisp-standalone-build.el) and a mechanical fix deserves its own targeted before/after measurement rather than a patch guessed at from this gate.") ("9	source-cache	0	1" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): the bare-file substrate's `mod'/`%%' compute a C-style truncating remainder (sign follows the dividend) instead of Elisp's floor-based `mod' (sign follows the divisor), and `floor'/`truncate' silently ignore a 2-argument divisor and return the dividend unchanged -- a silent wrong answer, not a crash. Confirmed by direct exit-code probes outside this gate: bare `(mod 7 -3)' returns 1, not -2; bare `(floor 7 2)' returns 7, not 3. Every other substrate, including host Emacs, agrees and is correct. Not fixed here: the wrong dispatch lives in the bare-mode AOT driver's low-level arithmetic table (scripts/nelisp-standalone-build.el) and a mechanical fix deserves its own targeted before/after measurement rather than a patch guessed at from this gate.") ("9	source-fallback	0	CRASH:void-function:probe-emit" . "KNOWN DEFECT (2026-08-22, NEW, found by this gate): eval-elisp-source's pure-source fallback path (no adjacent .neln artifact, cache-enable marker absent) does not share environment between its load step and its eval-forms step -- a function `defun'd by the loaded source file is void-function when a later FORM argument calls it. Every one of this corpus's 35 forms hits it the same way (the shim's own `probe-emit' goes void), so this reads as one failure repeated across the corpus, not 35 different ones. Reproduced in isolation outside this gate on a brand-new file that never had the cache enabled and never had an adjacent artifact, which rules out a stale-artifact leftover as the cause. `nelisp-performance-gate' does not catch this because it always pre-compiles an adjacent .neln before exercising the fallback path (see tools/nelisp-performance-gate.sh); this gate is the first to exercise pure source interpretation with nothing adjacent on disk. Not fixed here: this is inside `eval-elisp-source's own load/eval plumbing in lisp/nelisp-artifact.el and deserves its own targeted fix and regression test, not a patch guessed at from a parity gate.")) :keys ("0	source-fallback	1	CRASH:void-function:probe-emit" "1	source-fallback	1	CRASH:void-function:probe-emit" "10	source-fallback	1	CRASH:void-function:probe-emit" "11	artifact	0	1" "11	host	0	1" "11	load	0	1" "11	runtime-image	0	1" "11	source-cache	0	1" "11	source-fallback	0	CRASH:void-function:probe-emit" "12	source-fallback	1	CRASH:void-function:probe-emit" "13	artifact	0	1" "13	host	0	1" "13	load	0	1" "13	runtime-image	0	1" "13	source-cache	0	1" "13	source-fallback	0	CRASH:void-function:probe-emit" "14	artifact	0	1" "14	host	0	1" "14	load	0	1" "14	runtime-image	0	1" "14	source-cache	0	1" "14	source-fallback	0	CRASH:void-function:probe-emit" "15	artifact	CRASH:void-function:string-match	0" "15	host	CRASH:void-function:string-match	1" "15	load	CRASH:void-function:string-match	0" "15	runtime-image	CRASH:void-function:string-match	0" "15	source-cache	CRASH:void-function:string-match	0" "15	source-fallback	CRASH:void-function:string-match	CRASH:void-function:probe-emit" "16	source-fallback	1	CRASH:void-function:probe-emit" "17	source-fallback	1	CRASH:void-function:probe-emit" "18	artifact	CRASH:void-function:nreverse	0" "18	host	CRASH:void-function:nreverse	0" "18	load	CRASH:void-function:nreverse	0" "18	runtime-image	CRASH:void-function:nreverse	0" "18	source-cache	CRASH:void-function:nreverse	0" "18	source-fallback	CRASH:void-function:nreverse	CRASH:void-function:probe-emit" "19	artifact	CRASH:void-function:read	1" "19	host	CRASH:void-function:read	1" "19	load	CRASH:void-function:read	1" "19	runtime-image	CRASH:void-function:read	1" "19	source-cache	CRASH:void-function:read	1" "19	source-fallback	CRASH:void-function:read	CRASH:void-function:probe-emit" "2	artifact	CRASH:void-function:intern-soft	1" "2	host	CRASH:void-function:intern-soft	1" "2	load	CRASH:void-function:intern-soft	1" "2	runtime-image	CRASH:void-function:intern-soft	1" "2	source-cache	CRASH:void-function:intern-soft	1" "2	source-fallback	CRASH:void-function:intern-soft	CRASH:void-function:probe-emit" "20	host	CRASH:void-function:emacs-pid	1" "20	source-fallback	CRASH:void-function:emacs-pid	CRASH:void-function:probe-emit" "21	artifact	CRASH:void-function:random	1" "21	host	CRASH:void-function:random	1" "21	load	CRASH:void-function:random	1" "21	runtime-image	CRASH:void-function:random	1" "21	source-cache	CRASH:void-function:random	1" "21	source-fallback	CRASH:void-function:random	CRASH:void-function:probe-emit" "22	artifact	CRASH:void-function:make-temp-name	1" "22	host	CRASH:void-function:make-temp-name	1" "22	load	CRASH:void-function:make-temp-name	1" "22	runtime-image	CRASH:void-function:make-temp-name	1" "22	source-cache	CRASH:void-function:make-temp-name	1" "22	source-fallback	CRASH:void-function:make-temp-name	CRASH:void-function:probe-emit" "23	source-fallback	1	CRASH:void-function:probe-emit" "24	artifact	0	1" "24	host	0	1" "24	load	0	1" "24	runtime-image	0	1" "24	source-cache	0	1" "24	source-fallback	0	CRASH:void-function:probe-emit" "25	artifact	0	1" "25	host	0	1" "25	load	0	1" "25	runtime-image	0	1" "25	source-cache	0	1" "25	source-fallback	0	CRASH:void-function:probe-emit" "26	artifact	0	1" "26	host	0	1" "26	load	0	1" "26	runtime-image	0	1" "26	source-cache	0	1" "26	source-fallback	0	CRASH:void-function:probe-emit" "27	artifact	0	1" "27	host	0	1" "27	load	0	1" "27	runtime-image	0	1" "27	source-cache	0	1" "27	source-fallback	0	CRASH:void-function:probe-emit" "28	artifact	0	1" "28	host	0	1" "28	load	0	1" "28	runtime-image	0	1" "28	source-cache	0	1" "28	source-fallback	0	CRASH:void-function:probe-emit" "29	source-fallback	1	CRASH:void-function:probe-emit" "3	artifact	CRASH:void-function:intern-soft	1" "3	host	CRASH:void-function:intern-soft	1" "3	load	CRASH:void-function:intern-soft	1" "3	runtime-image	CRASH:void-function:intern-soft	1" "3	source-cache	CRASH:void-function:intern-soft	1" "3	source-fallback	CRASH:void-function:intern-soft	CRASH:void-function:probe-emit" "30	source-fallback	1	CRASH:void-function:probe-emit" "31	source-fallback	1	CRASH:void-function:probe-emit" "32	artifact	0	1" "32	host	0	1" "32	load	0	1" "32	runtime-image	0	1" "32	source-cache	0	1" "32	source-fallback	0	CRASH:void-function:probe-emit" "33	artifact	0	1" "33	host	0	1" "33	load	0	1" "33	runtime-image	0	1" "33	source-cache	0	1" "33	source-fallback	0	CRASH:void-function:probe-emit" "34	source-fallback	1	CRASH:void-function:probe-emit" "4	artifact	CRASH:void-function:read-from-string	1" "4	host	CRASH:void-function:read-from-string	1" "4	load	CRASH:void-function:read-from-string	1" "4	runtime-image	CRASH:void-function:read-from-string	1" "4	source-cache	CRASH:void-function:read-from-string	1" "4	source-fallback	CRASH:void-function:read-from-string	CRASH:void-function:probe-emit" "5	artifact	CRASH:void-function:read-from-string	1" "5	host	CRASH:void-function:read-from-string	1" "5	load	CRASH:void-function:read-from-string	1" "5	runtime-image	CRASH:void-function:read-from-string	1" "5	source-cache	CRASH:void-function:read-from-string	1" "5	source-fallback	CRASH:void-function:read-from-string	CRASH:void-function:probe-emit" "6	source-fallback	1	CRASH:void-function:probe-emit" "7	source-fallback	1	CRASH:void-function:probe-emit" "8	host	0	1" "8	source-fallback	0	CRASH:void-function:probe-emit" "9	artifact	0	1" "9	host	0	1" "9	load	0	1" "9	runtime-image	0	1" "9	source-cache	0	1" "9	source-fallback	0	CRASH:void-function:probe-emit"))
