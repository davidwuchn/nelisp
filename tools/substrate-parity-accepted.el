;; substrate-parity accepted divergences -- generated, review before commit.
;; Regenerate with NELISP_SUBSTRATE_PARITY_WRITE=1 (see the make
;; target); do not hand-add keys to silence a finding.
;;
;; Each key is "INDEX\tSUBSTRATE\tBASELINE\tACTUAL" -- both sides
;; of the divergence, not just its existence.  If either value
;; changes the key stops matching, the old entry goes stale, and
;; the new shape comes back as an unaccepted finding instead of
;; being silently covered (the ns-gate lesson).
;;
;; :notes is an alist of key -> why THIS entry is accepted, and it
;; survives regeneration.
(:generated-at "2026-08-22" :reason "Regenerated from a reviewed tree." :notes (("10	source-fallback	1	CRASH:void-function::" . "KNOWN GAP (2026-08-22, exposed by the eval-elisp-source pure-source-fallback file-read fix -- see src/nelisp-core-fileio.el): source-fallback is the only substrate whose FORM arguments (and, now that the file-read is fixed, its loaded FILE's own top-level forms too) run through the self-hosted `nelisp-eval' rather than through the native evaluator every other substrate calls directly. `nelisp--primitive-symbols' (src/nelisp-eval.el) is the list `nelisp--install-primitives' bulk-copies from native `fboundp' cells into `nelisp-eval's own function table, and it omits `floor', `%', `unibyte-string', `match-data', and `nelisp--write-stdout-bytes' -- all present natively (every other substrate agrees on the corpus's intended value), void-function only where `nelisp-eval' has to look them up itself. Not fixed here: completing `nelisp--primitive-symbols' is a separate, broader prelude-parity sweep, not a defect in the file-loading mechanism this commit repairs.") ("12	source-fallback	1	CRASH:void-function::" . "KNOWN GAP (2026-08-22, exposed by the eval-elisp-source pure-source-fallback file-read fix -- see src/nelisp-core-fileio.el): source-fallback is the only substrate whose FORM arguments (and, now that the file-read is fixed, its loaded FILE's own top-level forms too) run through the self-hosted `nelisp-eval' rather than through the native evaluator every other substrate calls directly. `nelisp--primitive-symbols' (src/nelisp-eval.el) is the list `nelisp--install-primitives' bulk-copies from native `fboundp' cells into `nelisp-eval's own function table, and it omits `floor', `%', `unibyte-string', `match-data', and `nelisp--write-stdout-bytes' -- all present natively (every other substrate agrees on the corpus's intended value), void-function only where `nelisp-eval' has to look them up itself. Not fixed here: completing `nelisp--primitive-symbols' is a separate, broader prelude-parity sweep, not a defect in the file-loading mechanism this commit repairs.") ("13	source-fallback	1	CRASH:void-function:floor" . "KNOWN GAP (2026-08-22, exposed by the eval-elisp-source pure-source-fallback file-read fix -- see src/nelisp-core-fileio.el -- and, for six of these forms, by the separate bare-file prelude-unification fix that stopped masking them behind a matching bare-side crash): source-fallback is the only substrate whose FORM arguments (and its loaded FILE's own top-level forms) run through the self-hosted `nelisp-eval' rather than through the native evaluator every other substrate calls directly. `nelisp--primitive-symbols' (src/nelisp-eval.el) is the list `nelisp--install-primitives' bulk-copies from native `fboundp' cells into `nelisp-eval's own function table, and it omits `floor', `%', `unibyte-string', `match-data', `nelisp--write-stdout-bytes', `intern-soft', `read-from-string', `read', `make-temp-name', and `prin1' -- all present natively (every other substrate agrees on the corpus's intended value), void-function (or `fboundp'-false) only where `nelisp-eval' has to look them up itself. Not fixed here: completing `nelisp--primitive-symbols' is a separate, broader prelude-parity sweep, not a defect in either the file-loading mechanism or the bare-file entry point this tree's two prelude fixes repair.") ("14	source-fallback	1	CRASH:void-function:floor" . "KNOWN GAP (2026-08-22, exposed by the eval-elisp-source pure-source-fallback file-read fix -- see src/nelisp-core-fileio.el -- and, for six of these forms, by the separate bare-file prelude-unification fix that stopped masking them behind a matching bare-side crash): source-fallback is the only substrate whose FORM arguments (and its loaded FILE's own top-level forms) run through the self-hosted `nelisp-eval' rather than through the native evaluator every other substrate calls directly. `nelisp--primitive-symbols' (src/nelisp-eval.el) is the list `nelisp--install-primitives' bulk-copies from native `fboundp' cells into `nelisp-eval's own function table, and it omits `floor', `%', `unibyte-string', `match-data', `nelisp--write-stdout-bytes', `intern-soft', `read-from-string', `read', `make-temp-name', and `prin1' -- all present natively (every other substrate agrees on the corpus's intended value), void-function (or `fboundp'-false) only where `nelisp-eval' has to look them up itself. Not fixed here: completing `nelisp--primitive-symbols' is a separate, broader prelude-parity sweep, not a defect in either the file-loading mechanism or the bare-file entry point this tree's two prelude fixes repair.") ("15	host	0	1" . "GAP vs host Emacs, consistent across every NeLisp substrate that can run this form -- not a cross-substrate NeLisp divergence. Form 20 (`emacs-pid') in particular is void-function in EVERY NeLisp substrate alike (bare, --load, runtime-image, artifact, source-cache), so it is a missing-primitive backlog item versus host Emacs, not a parity defect between NeLisp entry points.") ("15	source-fallback	0	CRASH:void-function:match-data" . "KNOWN GAP (2026-08-22, exposed by the eval-elisp-source pure-source-fallback file-read fix -- see src/nelisp-core-fileio.el -- and, for six of these forms, by the separate bare-file prelude-unification fix that stopped masking them behind a matching bare-side crash): source-fallback is the only substrate whose FORM arguments (and its loaded FILE's own top-level forms) run through the self-hosted `nelisp-eval' rather than through the native evaluator every other substrate calls directly. `nelisp--primitive-symbols' (src/nelisp-eval.el) is the list `nelisp--install-primitives' bulk-copies from native `fboundp' cells into `nelisp-eval's own function table, and it omits `floor', `%', `unibyte-string', `match-data', `nelisp--write-stdout-bytes', `intern-soft', `read-from-string', `read', `make-temp-name', and `prin1' -- all present natively (every other substrate agrees on the corpus's intended value), void-function (or `fboundp'-false) only where `nelisp-eval' has to look them up itself. Not fixed here: completing `nelisp--primitive-symbols' is a separate, broader prelude-parity sweep, not a defect in either the file-loading mechanism or the bare-file entry point this tree's two prelude fixes repair.") ("19	source-fallback	1	CRASH:void-function:read" . "KNOWN GAP (2026-08-22, exposed by the eval-elisp-source pure-source-fallback file-read fix -- see src/nelisp-core-fileio.el -- and, for six of these forms, by the separate bare-file prelude-unification fix that stopped masking them behind a matching bare-side crash): source-fallback is the only substrate whose FORM arguments (and its loaded FILE's own top-level forms) run through the self-hosted `nelisp-eval' rather than through the native evaluator every other substrate calls directly. `nelisp--primitive-symbols' (src/nelisp-eval.el) is the list `nelisp--install-primitives' bulk-copies from native `fboundp' cells into `nelisp-eval's own function table, and it omits `floor', `%', `unibyte-string', `match-data', `nelisp--write-stdout-bytes', `intern-soft', `read-from-string', `read', `make-temp-name', and `prin1' -- all present natively (every other substrate agrees on the corpus's intended value), void-function (or `fboundp'-false) only where `nelisp-eval' has to look them up itself. Not fixed here: completing `nelisp--primitive-symbols' is a separate, broader prelude-parity sweep, not a defect in either the file-loading mechanism or the bare-file entry point this tree's two prelude fixes repair.") ("2	source-fallback	1	CRASH:void-function:intern-soft" . "KNOWN GAP (2026-08-22, exposed by the eval-elisp-source pure-source-fallback file-read fix -- see src/nelisp-core-fileio.el -- and, for six of these forms, by the separate bare-file prelude-unification fix that stopped masking them behind a matching bare-side crash): source-fallback is the only substrate whose FORM arguments (and its loaded FILE's own top-level forms) run through the self-hosted `nelisp-eval' rather than through the native evaluator every other substrate calls directly. `nelisp--primitive-symbols' (src/nelisp-eval.el) is the list `nelisp--install-primitives' bulk-copies from native `fboundp' cells into `nelisp-eval's own function table, and it omits `floor', `%', `unibyte-string', `match-data', `nelisp--write-stdout-bytes', `intern-soft', `read-from-string', `read', `make-temp-name', and `prin1' -- all present natively (every other substrate agrees on the corpus's intended value), void-function (or `fboundp'-false) only where `nelisp-eval' has to look them up itself. Not fixed here: completing `nelisp--primitive-symbols' is a separate, broader prelude-parity sweep, not a defect in either the file-loading mechanism or the bare-file entry point this tree's two prelude fixes repair.") ("20	host	CRASH:void-function:emacs-pid	1" . "GAP vs host Emacs, consistent across every NeLisp substrate that can run this form -- not a cross-substrate NeLisp divergence. Form 20 (`emacs-pid') in particular is void-function in EVERY NeLisp substrate alike (bare, --load, runtime-image, artifact, source-cache), so it is a missing-primitive backlog item versus host Emacs, not a parity defect between NeLisp entry points.") ("22	source-fallback	1	CRASH:void-function:make-temp-name" . "KNOWN GAP (2026-08-22, exposed by the eval-elisp-source pure-source-fallback file-read fix -- see src/nelisp-core-fileio.el -- and, for six of these forms, by the separate bare-file prelude-unification fix that stopped masking them behind a matching bare-side crash): source-fallback is the only substrate whose FORM arguments (and its loaded FILE's own top-level forms) run through the self-hosted `nelisp-eval' rather than through the native evaluator every other substrate calls directly. `nelisp--primitive-symbols' (src/nelisp-eval.el) is the list `nelisp--install-primitives' bulk-copies from native `fboundp' cells into `nelisp-eval's own function table, and it omits `floor', `%', `unibyte-string', `match-data', `nelisp--write-stdout-bytes', `intern-soft', `read-from-string', `read', `make-temp-name', and `prin1' -- all present natively (every other substrate agrees on the corpus's intended value), void-function (or `fboundp'-false) only where `nelisp-eval' has to look them up itself. Not fixed here: completing `nelisp--primitive-symbols' is a separate, broader prelude-parity sweep, not a defect in either the file-loading mechanism or the bare-file entry point this tree's two prelude fixes repair.") ("25	source-fallback	1	0" . "KNOWN GAP (2026-08-22, exposed by the eval-elisp-source pure-source-fallback file-read fix -- see src/nelisp-core-fileio.el -- and, for six of these forms, by the separate bare-file prelude-unification fix that stopped masking them behind a matching bare-side crash): source-fallback is the only substrate whose FORM arguments (and its loaded FILE's own top-level forms) run through the self-hosted `nelisp-eval' rather than through the native evaluator every other substrate calls directly. `nelisp--primitive-symbols' (src/nelisp-eval.el) is the list `nelisp--install-primitives' bulk-copies from native `fboundp' cells into `nelisp-eval's own function table, and it omits `floor', `%', `unibyte-string', `match-data', `nelisp--write-stdout-bytes', `intern-soft', `read-from-string', `read', `make-temp-name', and `prin1' -- all present natively (every other substrate agrees on the corpus's intended value), void-function (or `fboundp'-false) only where `nelisp-eval' has to look them up itself. Not fixed here: completing `nelisp--primitive-symbols' is a separate, broader prelude-parity sweep, not a defect in either the file-loading mechanism or the bare-file entry point this tree's two prelude fixes repair.") ("27	source-fallback	1	0" . "KNOWN GAP (2026-08-22, exposed by the eval-elisp-source pure-source-fallback file-read fix -- see src/nelisp-core-fileio.el -- and, for six of these forms, by the separate bare-file prelude-unification fix that stopped masking them behind a matching bare-side crash): source-fallback is the only substrate whose FORM arguments (and its loaded FILE's own top-level forms) run through the self-hosted `nelisp-eval' rather than through the native evaluator every other substrate calls directly. `nelisp--primitive-symbols' (src/nelisp-eval.el) is the list `nelisp--install-primitives' bulk-copies from native `fboundp' cells into `nelisp-eval's own function table, and it omits `floor', `%', `unibyte-string', `match-data', `nelisp--write-stdout-bytes', `intern-soft', `read-from-string', `read', `make-temp-name', and `prin1' -- all present natively (every other substrate agrees on the corpus's intended value), void-function (or `fboundp'-false) only where `nelisp-eval' has to look them up itself. Not fixed here: completing `nelisp--primitive-symbols' is a separate, broader prelude-parity sweep, not a defect in either the file-loading mechanism or the bare-file entry point this tree's two prelude fixes repair.") ("28	source-fallback	1	0" . "KNOWN GAP (2026-08-22, exposed by the eval-elisp-source pure-source-fallback file-read fix -- see src/nelisp-core-fileio.el -- and, for six of these forms, by the separate bare-file prelude-unification fix that stopped masking them behind a matching bare-side crash): source-fallback is the only substrate whose FORM arguments (and its loaded FILE's own top-level forms) run through the self-hosted `nelisp-eval' rather than through the native evaluator every other substrate calls directly. `nelisp--primitive-symbols' (src/nelisp-eval.el) is the list `nelisp--install-primitives' bulk-copies from native `fboundp' cells into `nelisp-eval's own function table, and it omits `floor', `%', `unibyte-string', `match-data', `nelisp--write-stdout-bytes', `intern-soft', `read-from-string', `read', `make-temp-name', and `prin1' -- all present natively (every other substrate agrees on the corpus's intended value), void-function (or `fboundp'-false) only where `nelisp-eval' has to look them up itself. Not fixed here: completing `nelisp--primitive-symbols' is a separate, broader prelude-parity sweep, not a defect in either the file-loading mechanism or the bare-file entry point this tree's two prelude fixes repair.") ("3	source-fallback	1	CRASH:void-function:intern-soft" . "KNOWN GAP (2026-08-22, exposed by the eval-elisp-source pure-source-fallback file-read fix -- see src/nelisp-core-fileio.el -- and, for six of these forms, by the separate bare-file prelude-unification fix that stopped masking them behind a matching bare-side crash): source-fallback is the only substrate whose FORM arguments (and its loaded FILE's own top-level forms) run through the self-hosted `nelisp-eval' rather than through the native evaluator every other substrate calls directly. `nelisp--primitive-symbols' (src/nelisp-eval.el) is the list `nelisp--install-primitives' bulk-copies from native `fboundp' cells into `nelisp-eval's own function table, and it omits `floor', `%', `unibyte-string', `match-data', `nelisp--write-stdout-bytes', `intern-soft', `read-from-string', `read', `make-temp-name', and `prin1' -- all present natively (every other substrate agrees on the corpus's intended value), void-function (or `fboundp'-false) only where `nelisp-eval' has to look them up itself. Not fixed here: completing `nelisp--primitive-symbols' is a separate, broader prelude-parity sweep, not a defect in either the file-loading mechanism or the bare-file entry point this tree's two prelude fixes repair.") ("32	source-fallback	1	0" . "KNOWN GAP (2026-08-22, exposed by the eval-elisp-source pure-source-fallback file-read fix -- see src/nelisp-core-fileio.el -- and, for six of these forms, by the separate bare-file prelude-unification fix that stopped masking them behind a matching bare-side crash): source-fallback is the only substrate whose FORM arguments (and its loaded FILE's own top-level forms) run through the self-hosted `nelisp-eval' rather than through the native evaluator every other substrate calls directly. `nelisp--primitive-symbols' (src/nelisp-eval.el) is the list `nelisp--install-primitives' bulk-copies from native `fboundp' cells into `nelisp-eval's own function table, and it omits `floor', `%', `unibyte-string', `match-data', `nelisp--write-stdout-bytes', `intern-soft', `read-from-string', `read', `make-temp-name', and `prin1' -- all present natively (every other substrate agrees on the corpus's intended value), void-function (or `fboundp'-false) only where `nelisp-eval' has to look them up itself. Not fixed here: completing `nelisp--primitive-symbols' is a separate, broader prelude-parity sweep, not a defect in either the file-loading mechanism or the bare-file entry point this tree's two prelude fixes repair.") ("34	source-fallback	1	0" . "KNOWN GAP (2026-08-22, exposed by the eval-elisp-source pure-source-fallback file-read fix -- see src/nelisp-core-fileio.el): source-fallback is the only substrate whose FORM arguments (and, now that the file-read is fixed, its loaded FILE's own top-level forms too) run through the self-hosted `nelisp-eval' rather than through the native evaluator every other substrate calls directly. `nelisp--primitive-symbols' (src/nelisp-eval.el) is the list `nelisp--install-primitives' bulk-copies from native `fboundp' cells into `nelisp-eval's own function table, and it omits `floor', `%', `unibyte-string', `match-data', and `nelisp--write-stdout-bytes' -- all present natively (every other substrate agrees on the corpus's intended value), void-function only where `nelisp-eval' has to look them up itself. Not fixed here: completing `nelisp--primitive-symbols' is a separate, broader prelude-parity sweep, not a defect in the file-loading mechanism this commit repairs.") ("4	source-fallback	1	CRASH:void-function:read-from-string" . "KNOWN GAP (2026-08-22, exposed by the eval-elisp-source pure-source-fallback file-read fix -- see src/nelisp-core-fileio.el -- and, for six of these forms, by the separate bare-file prelude-unification fix that stopped masking them behind a matching bare-side crash): source-fallback is the only substrate whose FORM arguments (and its loaded FILE's own top-level forms) run through the self-hosted `nelisp-eval' rather than through the native evaluator every other substrate calls directly. `nelisp--primitive-symbols' (src/nelisp-eval.el) is the list `nelisp--install-primitives' bulk-copies from native `fboundp' cells into `nelisp-eval's own function table, and it omits `floor', `%', `unibyte-string', `match-data', `nelisp--write-stdout-bytes', `intern-soft', `read-from-string', `read', `make-temp-name', and `prin1' -- all present natively (every other substrate agrees on the corpus's intended value), void-function (or `fboundp'-false) only where `nelisp-eval' has to look them up itself. Not fixed here: completing `nelisp--primitive-symbols' is a separate, broader prelude-parity sweep, not a defect in either the file-loading mechanism or the bare-file entry point this tree's two prelude fixes repair.") ("5	source-fallback	1	CRASH:void-function:read-from-string" . "KNOWN GAP (2026-08-22, exposed by the eval-elisp-source pure-source-fallback file-read fix -- see src/nelisp-core-fileio.el -- and, for six of these forms, by the separate bare-file prelude-unification fix that stopped masking them behind a matching bare-side crash): source-fallback is the only substrate whose FORM arguments (and its loaded FILE's own top-level forms) run through the self-hosted `nelisp-eval' rather than through the native evaluator every other substrate calls directly. `nelisp--primitive-symbols' (src/nelisp-eval.el) is the list `nelisp--install-primitives' bulk-copies from native `fboundp' cells into `nelisp-eval's own function table, and it omits `floor', `%', `unibyte-string', `match-data', `nelisp--write-stdout-bytes', `intern-soft', `read-from-string', `read', `make-temp-name', and `prin1' -- all present natively (every other substrate agrees on the corpus's intended value), void-function (or `fboundp'-false) only where `nelisp-eval' has to look them up itself. Not fixed here: completing `nelisp--primitive-symbols' is a separate, broader prelude-parity sweep, not a defect in either the file-loading mechanism or the bare-file entry point this tree's two prelude fixes repair.") ("7	source-fallback	1	CRASH:void-function:unibyte-string" . "KNOWN GAP (2026-08-22, exposed by the eval-elisp-source pure-source-fallback file-read fix -- see src/nelisp-core-fileio.el): source-fallback is the only substrate whose FORM arguments (and, now that the file-read is fixed, its loaded FILE's own top-level forms too) run through the self-hosted `nelisp-eval' rather than through the native evaluator every other substrate calls directly. `nelisp--primitive-symbols' (src/nelisp-eval.el) is the list `nelisp--install-primitives' bulk-copies from native `fboundp' cells into `nelisp-eval's own function table, and it omits `floor', `%', `unibyte-string', `match-data', and `nelisp--write-stdout-bytes' -- all present natively (every other substrate agrees on the corpus's intended value), void-function only where `nelisp-eval' has to look them up itself. Not fixed here: completing `nelisp--primitive-symbols' is a separate, broader prelude-parity sweep, not a defect in the file-loading mechanism this commit repairs.") ("8	host	0	1" . "ACCEPTED (by design, class ii): NeLisp strings are Rust UTF-8 internally, so a raw byte >= 128 written through `unibyte-string' round-trips through UTF-8 re-encoding -- `aref' does not return the original byte the way host Emacs's unibyte strings do. Documented in memory feedback_nelisp_standalone_utf8_raw_byte_block. Every NeLisp substrate agrees with every other NeLisp substrate here; only the comparison against host Emacs differs, which is exactly the documented limitation.") ("8	source-fallback	0	CRASH:void-function:unibyte-string" . "KNOWN GAP (2026-08-22, exposed by the eval-elisp-source pure-source-fallback file-read fix -- see src/nelisp-core-fileio.el): source-fallback is the only substrate whose FORM arguments (and, now that the file-read is fixed, its loaded FILE's own top-level forms too) run through the self-hosted `nelisp-eval' rather than through the native evaluator every other substrate calls directly. `nelisp--primitive-symbols' (src/nelisp-eval.el) is the list `nelisp--install-primitives' bulk-copies from native `fboundp' cells into `nelisp-eval's own function table, and it omits `floor', `%', `unibyte-string', `match-data', and `nelisp--write-stdout-bytes' -- all present natively (every other substrate agrees on the corpus's intended value), void-function only where `nelisp-eval' has to look them up itself. Not fixed here: completing `nelisp--primitive-symbols' is a separate, broader prelude-parity sweep, not a defect in the file-loading mechanism this commit repairs.")) :keys ("10	source-fallback	1	CRASH:void-function::" "12	source-fallback	1	CRASH:void-function::" "13	source-fallback	1	CRASH:void-function:floor" "14	source-fallback	1	CRASH:void-function:floor" "15	host	0	1" "15	source-fallback	0	CRASH:void-function:match-data" "19	source-fallback	1	CRASH:void-function:read" "2	source-fallback	1	CRASH:void-function:intern-soft" "20	host	CRASH:void-function:emacs-pid	1" "22	source-fallback	1	CRASH:void-function:make-temp-name" "25	source-fallback	1	0" "27	source-fallback	1	0" "28	source-fallback	1	0" "3	source-fallback	1	CRASH:void-function:intern-soft" "32	source-fallback	1	0" "34	source-fallback	1	0" "4	source-fallback	1	CRASH:void-function:read-from-string" "5	source-fallback	1	CRASH:void-function:read-from-string" "7	source-fallback	1	CRASH:void-function:unibyte-string" "8	host	0	1" "8	source-fallback	0	CRASH:void-function:unibyte-string"))
