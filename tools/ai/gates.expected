# Gates that must have produced a report before the tree can be called good.
#
# This file is the half of the mechanism that an exit code cannot supply.
# A gate that never runs leaves no report; without a list of gates that
# ought to exist, "no report" and "nothing to report" are the same thing.
# A CI gate in this repository sat behind an already-red target for its
# whole life and was counted as passing the entire time.
#
# One gate name per line.  A trailing `?' marks the gate optional: it is
# shown when present, and its absence is not a failure.
#
# Add a line here the moment a gate becomes load-bearing.  Removing a
# line is a decision to stop caring about that gate — make it explicitly,
# not by letting the gate quietly stop emitting.

compile
ert-full

# Tree-wide checks that already ratchet against a baseline.  Each now
# prints a GATE-COUNT line so "clean" and "scanned nothing" can be told
# apart; run them through `nelisp-ai.sh gate NAME -- make TARGET'.
parens-check
unsafe-inventory
ns-inventory
reader-surface-audit
pkg-graph
pkg-load-lists

# Developer-loop gates: useful, but not evidence about the whole tree.
ert-focus?
ns?

# Namespace boundaries for the recipe skeletons.  Whole-tree namespace
# checking is `make ns-inventory', which ratchets against a baseline;
# this one requires the skeletons to be simply clean, since they are
# what people copy.
ns-recipes?

# Recipe smokes: run them when touching recipes/, or to find out whether
# an application shape still works on this machine.
recipe-stdio-service?
recipe-batch-data?
recipe-native-hotpath?
recipe-native-hotpath-exec?
recipe-checked-resources?

# Measurements.  A bench gate passes when the ratio is valid, skips with
# a reason when the machine invalidated it, and fails only when the two
# arms are the same program.  It never fails on the number itself.
bench-borrow-check?

# Standalone gates.  These build a binary from the tree and run it, so
# they need a Linux host; on Windows they now report a reasoned skip
# (GATE-SKIP) rather than exiting 0, which used to read as a pass.
#
# standalone-reader-test is OPTIONAL because it is currently red on
# Linux, and was before this contract reached it.  Verified 2026-08-18 in
# WSL Debian on both this branch and plain main, failing identically at
# the fifth of its eight checks:
#
#   GATE-COUNT checked=5 findings=1
#   FAIL: command smoke: eval-elisp-artifact exit=1
#         "nelisp: uncaught error: file-missing: url-parse"
#
# Cause: `require' now signals `file-missing' instead of silently
# succeeding (a good change), and the standalone path carries requires
# that only ever worked because it did not.  src/nelisp-eval.el:101
# requires the host library `url-parse', which the standalone runtime
# does not have; `make pkg-graph' independently lists url-parse among
# the requires nothing in the tree provides.
#
# It is a chain, not a line.  Guarding the url-* requires with
# `ignore-errors' (measured in a throwaway WSL clone) moves the failure
# on to `file-missing: nelisp-core-fileio', which is a tree file, so the
# remaining question is how the standalone resolves its own requires --
# a policy decision for whoever owns that path, not a patch to guess at
# from here.  `locate-library' is void in the standalone, so that guard
# is not available either.
#
# nelisp-native-artifact-gate fails at `load_source_uses_neln' with the
# same error and is left unwired for the same reason.
#
# Required-vs-optional here is a judgement, not an oversight: making it
# required would paint every Linux verify red for a defect this harness
# only reports.  Promote the line once the require chain is resolved.
standalone-reader-test?
#
# Not yet wired, and not wired blind: adding a wrapper whose success
# path has never run would be the same unverified-gate problem in a new
# place.
#
# To adopt one, on a machine that can run it: give the script a
# GATE-COUNT line where it already knows its case count, make the
# can't-run path print a reason instead of failing, wrap it with
# `nelisp-ai.sh gate', and move the name above this comment.
#
# standalone-reader-test
# standalone-selfhost-test
# nelisp-performance-gate
# nelisp-native-artifact-gate
#
# `make audit' is deliberately absent: it exits 1 on 26 PENDING
# sub-phases, which is a roadmap state rather than a defect, and a gate
# that is red by design trains people to ignore the column.
